# 벨챗 v2.9.5 — Fact 확보 시 낭비 제거

2026-08-17 · `vela-boardroom-prototype.html`

한 줄 요약: **답을 이미 아는 질문에 무료 한도를 여섯 번 쓰던 걸 한 번으로 줄였다. 그리고 서킷의 우회로를 막았다.**

---

## 1. 실측 로그가 보여준 것

```
📡 데이터API 성공: 환율      ← 1,411원을 이미 손에 쥐었다
🚫 신선도:환율               ← 그런데 여전히 "검색을 원함" 으로 남아 있다
✕ ✕ ✕ ✕ ✕ ✕                ← LLM 을 6회 재시도
⛔ Gemini 차단 중 (42초 남음)  ← 차단 중인데 같은 시각에 ✕ 가 찍혔다
```

세 가지가 한꺼번에 드러났다. 하나씩 보면 사소한데, 겹치면 **이미 답을 아는 질문에
무료 한도를 여섯 번 태우는** 결과가 된다.

---

## 2. 서킷 OPEN 중 호출 — 순서 문제인가, 실호출인가

### 판별 결과: **실호출이다.** 순서 문제가 아니었다.

`resolveRoute` 의 auto 경로를 보면 원인이 분명하다.

```js
const usable = order.filter(p => exclude.indexOf(p) < 0 && providerKeyOf(p));
                                 // ↑ 키 유무만 본다. 서킷은 안 본다
const ready = usable.filter(providerAvailable);   // 여기서만 서킷을 본다
if (ready.length) return build(ready[0]);
if (!usable.length) return null;
const soonest = usable.slice().sort(...)[0];
return build(soonest);          // ← ready 가 비면 **차단된 곳을 그대로 돌려준다**
```

`ready` 가 비는 순간 `soonest` 폴백이 서킷을 그냥 넘어간다.
**서킷을 달아놓고 그 옆에 우회로를 열어둔 셈이다.**

v2.9.3에서 `strictSearch` 경로만 이 폴백을 막았고, 일반 경로에는 손을 안 댄 게 원인이었다.

### 고친 방법

서킷이 열린 곳은 **후보 목록(`usable`)에서부터** 뺀다.

```js
const usable = order.filter(p =>
  exclude.indexOf(p) < 0 && providerKeyOf(p) && circuitAllows(p));
```

전부 차단이면 `null` 을 돌려준다. 호출부가 Fact 표시나 안내로 처리하고,
서킷은 `openMs`(기본 60초) 뒤 스스로 풀린다.

### 검증

```
전부 차단        → null (호출 안 함)        ✅
한 곳만 차단     → 정상인 곳으로 우회        ✅
쿨다운(서킷 아님) → 가장 빨리 풀리는 곳       ✅ (기존 동작 유지)
```

쿨다운과 서킷을 구분한 게 중요하다. **쿨다운은 "잠깐 쉬어라"라서 급하면 써도 되지만,
서킷은 "연속으로 실패했으니 지금은 안 된다"라서 쓰면 안 된다.**

---

## 3. Fact를 쥔 상태의 재시도 — 기다릴 이유가 없다

공식 값을 이미 받아온 턴은 급할 게 없다. **사용자에게 보여줄 숫자가 손에 있기 때문이다.**
LLM은 그 숫자에 설명을 붙이는 역할일 뿐이고, 설명은 없어도 답은 성립한다.

그런데 v2.9.4까지는 이런 턴에서도 평소와 똑같이 폴백·재시도를 다 태웠다.
**기다려서 얻는 건 설명 한 줄이고, 잃는 건 다음 질문에 쓸 한도다.** 교환이 성립하지 않는다.

### `FACT_RULES` 신설

```js
const FACT_RULES = {
  llmAttempts: 1,        // Fact 가 있을 때 LLM 호출을 몇 번까지 시도할지
  waitOnQuota: false     // 한도를 만나도 기다렸다 재시도할지 (false = 즉시 숫자 표시)
};
```

- **시도 1회.** 실패하면 폴백도 대기도 없이 곧바로 "숫자만 표시"로 전환한다.
- Fact가 **없는** 턴은 기존 그대로(최대 8회 + 대기 재시도). 이 변경은 Fact 턴에만 적용된다.

```
Fact 있음 : 최대 1회  → 실패 시 즉시 "조회된 환율은 1달러 = 1,411.27원이에요" + 출처줄
Fact 없음 : 최대 8회  → 기존 폴백·재시도 유지
```

### 안전장치

이미 스트림이 시작돼 글자가 나온 턴(`rec.spoke`)은 숫자 표시로 덮지 않는다.
사람이 말하다 만 걸 앱 문장으로 갈아끼우면 더 이상하다.

---

## 4. 검색 의사 자체를 내린다

v2.9.4는 Fact를 확보해도 `searchInfo.wanted` 가 `true` 로 남아 로그에 🚫(원했지만 못 함)가 찍혔다.
**실제로는 검색할 이유가 사라진 상태**인데 표시가 그걸 반영하지 못했다.

```
v2.9.4:  🔍 신선도:환율                              ← 검색을 원한 것으로 남음
v2.9.5:  📡 신선도:환율 → 데이터API로 해결(검색 불필요)
```

동작상으로도 `webSearch` 슬롯이 0이고 `needsSearch`·`strict` 가 모두 false라
**검색 시도도 검색 대기도 일어나지 않는다.** 이제 표시와 동작이 일치한다.

---

## 5. 종합 효과

환율 질문 한 번에 들어가는 LLM 호출:

```
v2.9.4:  데이터API 1 + LLM 6회 (폴백·재시도)  = 한도 6소모
v2.9.5:  데이터API 1 + LLM 1회               = 한도 1소모
```

그리고 그 1회마저 실패해도 **사용자는 숫자를 본다** (v2.9.4의 Fact 보존이 그대로 작동).
`CALLS_PER_MESSAGE` 기준으로 환율·날씨 턴은 **1.0** 에 정확히 수렴한다.

---

## 6. 검증

- 서킷 전부 차단 → `null` 반환, 실호출 없음
- 한 곳만 차단 → 정상 우회 / 쿨다운만 걸린 경우 → 기존 soonest 폴백 유지
- `FACT_RULES` 적용: Fact 있음 1회 / 없음 8회
- 검색 판정 표시가 동작과 일치 (📡 데이터API로 해결)
- v2.9.4 회귀 없음 — 문장 21개 구멍 0건, Validator 5케이스, 서킷 3회 OPEN·복구,
  대기 종료 4조건, 5인 잡담 로테이션·담당자 고정
- 문법 오류 0

**실기기 확인 미완** (브라우저 확장 미연결): 실제 429 상황에서 로그가
`📡 → ✕ 1회 → 숫자 표시` 로 끝나는지. 다음에 한도를 만나면 설정 → 🔀 최근 라우팅에서
✕ 개수가 1개인지 확인하면 이번 수정이 먹었는지 바로 알 수 있다.

---

## 7. 조정하는 법

| 원하는 것 | 방법 |
|---|---|
| Fact 턴에서도 폴백 한 번 더 | `FACT_RULES.llmAttempts = 2` |
| Fact 턴에서도 한도 대기 | `FACT_RULES.waitOnQuota = true` |
| 서킷을 덜 민감하게 | `CIRCUIT_RULES.failThreshold` 를 올린다 |
| 서킷 차단 시간 조절 | `CIRCUIT_RULES.openMs` (기본 60000) |
| 서킷 끄기 (v2.9.4 동작) | `CIRCUIT_RULES.enabled = false` |
