# 벨챗 v2.9.2 — 무료 한도 콜 다이어트 + 신선도 검색 완수 + 비상 화법 보정

2026-08-17 · `vela-boardroom-prototype.html`

한 줄 요약: **신선도 턴은 콜을 절반으로 줄이고, 대신 검색이 풀릴 때까지 기다렸다 끝까지 해낸다.**

---

## 1. 실측 로그가 말해준 것

환율 질문 한 번에 **Gemini 503 → Groq 429**로 검색이 불발됐고, 2분 사이 호출이 **12건** 나갔다.

원인을 뜯어보니 네 가지가 겹쳐 있었다. 하나씩 보면 사소한데, 겹치면 정확히 이 결과가 나온다.

| # | 문제 | 왜 나쁜가 |
|---|---|---|
| ① | 신선도 턴에 **2명이 응답** | 호출이 두 배. 두 번째 사람은 검색도 없이 같은 얘기를 반복하면서 분당 한도만 갉아먹는다 |
| ② | 검색 되는 곳이 막히면 **검색 못 하는 곳으로 양보** | Gemini가 죽자 Groq으로 내려갔다. 그런데 Groq은 검색을 못 한다 — 갈 이유가 없는 호출이었다 |
| ③ | **Retry-After의 분 단위 표기를 못 읽었다** | Groq은 `try again in 7m30s`로 알려주는데 초 단위만 파싱했다. 0으로 떨어져 4초 뒤 재시도 → 429를 한 건 더 생산 |
| ④ | 백오프가 **4초**에서 시작 | 무료 한도는 분 단위로 풀린다. 4초 뒤에 두드려서 풀릴 리가 없다 |

즉 **한도가 모자라서 실패한 게 아니라, 실패하는 방식이 한도를 더 빨리 태웠다.**

---

## 2. 신선도 턴 콜 다이어트

### 담당자 1명만 답한다 (`FRESHNESS_RULES.soloResponder`)

신선도 검색 턴은 응답자를 담당자 한 명으로 자른다.
두 번째 목소리가 필요하면 **검색 결과가 나온 뒤 연쇄 반응으로** 붙는 게 맞다 —
검색 결과도 없이 동시에 호출해봐야 같은 숫자 얘기를 반복할 뿐이고, 그 호출이 첫 사람을 막는다.

### 연쇄 반응 확률도 낮춘다 (`FRESHNESS_RULES.chainChance`)

기본 0.30 → 신선도 턴 **0.15**. `0`으로 두면 아예 안 붙는다.
이 턴은 이미 한도 압박 속에서 겨우 검색을 마친 상태다. 한 콜 더 쓰면 다음 질문이 막힌다.

### 결과

```
응답자      2명  →  1명
연쇄 반응   0.30 →  0.15
기대 콜 수  2.30건 → 1.15건   (50% 감소)
```

---

## 3. 검색 완수 우선 정책

신선도 턴은 성격이 다르다.

> 보통 턴: 답이 안 나오면 아쉽다
> 신선도 턴: **검색을 못 하면 답 자체가 무가치하다.** 틀린 숫자는 없느니만 못하다.

그래서 두 가지를 뒤집었다.

### ① 검색 못 하는 곳으로 내려가지 않는다 (`strictSearch`)

`resolveRoute` 에 `strictSearch` 를 넣었다. 신선도 턴에서는 검색 되는 곳이 전부 막혀도
Groq으로 양보하지 않고 **`null` 을 돌려준다** — 호출부가 "기다렸다 다시"로 넘어간다.

```
Gemini 를 이미 시도해 제외한 상태:
  strict 없음     → groq          (검색 불가. 실측 사고의 그 경로)
  strictSearch    → null (기다린다)
```

### ② Retry-After가 길어도 존중한다

재시도 횟수 대신 **대기 예산**으로 관리한다 (`waitCapMs`, 기본 90초).
저쪽이 "7분 뒤에 오라"고 하면 그건 진짜다. 30초 만에 다시 두드려봐야 429를 한 건 더 만들 뿐이다.
예산 안에서는 그 말을 그대로 따르고, 예산을 넘으면 그때만 검색을 포기한다.

기다리는 동안 말풍선 머리에 이렇게 뜬다:

```
🔍 사용량이 많아 47초 뒤 찾아볼게요
```

일반 재시도(`무료 한도 — N초 후 재시도`)와 **문구를 일부러 다르게 했다.**
사용자 입장에서 이건 "오류를 재시도하는 중"이 아니라 "찾아보려고 순서를 기다리는 중"이다.
기다림의 이유가 납득되면 같은 시간도 견딜 만해진다. 개발자 용어(429·API·한도)는 쓰지 않는다.

### 놓칠 뻔한 회귀 하나

기존 재시도 루프는 `for (guard = 0; guard < 8; guard++)` 였다.
여기에 대기 회차까지 그대로 넣었더니 **90초를 잘게 쪼개 기다리는 동안 시도 상한을 다 써버리고,
정작 한도가 풀린 뒤에 호출을 못 하는** 구조가 됐다. 기다린 보람 없이 실패한다.

→ `guard` 는 **실제 호출 횟수만** 세도록 바꿨다. 대기 쪽은 `waitedMs` 예산이 따로 끝을 보장한다.

### Groq만 쓰는 사용자는 기다리지 않는다

검색 가능한 키가 아예 없으면 `searchCapable()` 이 false라 애초에 `needsSearch` 가 서지 않는다.
따라서 strict 모드도 켜지지 않고 **즉시 답한다.** (그 답에는 v2.9.1의 "단정 금지" 규칙이 그대로 걸린다)

---

## 4. 재시도 간격 현실화

```
429:  base 4,000ms → 12,000ms   /  max 30,000ms → 60,000ms
503:  base 1,500ms →  3,000ms   /  max  4,000ms →  8,000ms
529:  동일
```

그리고 `parseRetryAfterMs` 가 분 단위를 읽는다:

```
"try again in 7m30.5s"   →  450,500ms   (v2.9.1 은 0 이었다)
"try again in 1m"        →   60,000ms
"try again in 42s"       →   42,000ms
"try again in 800ms"     →      800ms
'{"retryDelay":"25s"}'   →   25,000ms
```

`ms` 를 `s` 보다 **먼저** 검사하도록 순서도 바로잡았다 (`800ms` 가 `800s` 로 읽히던 잠재 버그).

---

## 5. 비상 화법 보정

실제로 이런 답이 나갔다: **"Open API 호출"**. 방에 앉아 있는 사람이 쓸 말이 아니다.
한 번 나오면 대화가 프로그램 오류창이 된다.

### 공통 규칙 추가 (`COMMON_RULES`)

```
[앱 사정을 사용자에게 설명하지 마라 — 너는 개발자가 아니다]
- 금지: "API 호출", "Open API", "JSON", "응답 오류", "서버 오류", "요청 실패",
  "토큰", "한도 초과", "429", "타임아웃", "파싱", "쿼리", "엔드포인트", "크롤링"
- 대신 사람 말로: "지금은 확인이 잘 안 되네요", "이따 다시 찾아볼게요"
- 못 찾았으면 다음에 뭘 하면 되는지 한 가지만 알려준다.
  실제로 있는 곳만 든다 — 네이버 검색, 은행 앱, 기상청 같은 것.
  주소(URL)를 지어내지 마라.
- 사과를 길게 늘어놓지 마라. 한 마디면 충분하고, 네 말투를 유지한다.
```

### 턴별 지시

**검색을 못 붙인 신선도 턴** — 요청하신 문구를 그대로 넣었다:

> "지금 숫자는 확인이 필요해요 — 잠시 뒤에 다시 물어봐 주시면 찾아볼게요" 정도로, 네 말투로 정직하게 말해라.
> 급하면 어디서 보면 되는지 **한 곳만** 자연스럽게 알려줘라 (네이버에서 그 단어로 검색, 은행 앱, 기상청 같은 실제로 있는 곳). 여러 개 나열하지 말고, 주소를 지어내지 마라.
> 왜 못 찾았는지는 설명하지 마라. "API", "호출", "오류" 같은 말을 쓰면 안 된다 — 넌 개발자가 아니라 이 방 사람이다.

**검색을 걸었는데 결과가 안 온 턴** — 지시는 턴 시작에 만들어지므로, 그 안에 미리 넣어뒀다:

> 만약 검색 결과가 실제로 오지 않았다면, 아는 숫자로 때우지 말고
> "지금은 확인이 잘 안 되네요, 잠시 뒤에 다시 물어봐 주시면 찾아볼게요" 정도로 네 말투로 말해라.
> 왜 안 됐는지 기술적으로 설명하지 마라 — 그건 네가 알 바가 아니고 듣는 사람도 궁금하지 않다.

---

## 6. 진단 로그 보강

기다리다 검색을 포기한 경우가 로그에 남는다:

```
🚫 신선도:환율 → 90초 기다렸지만 검색 실패
```

---

## 7. 검증

- Retry-After 해석 6종 전부 통과 (분 단위 포함)
- 라우팅: 실측 사고 경로(Gemini 제외 상태)에서 `strict` 가 Groq 양보를 막는 것 확인
- 콜 수: 2.30건 → 1.15건 (50% 감소)
- 시도 상한 회귀 수정 — 대기 회차가 호출 상한을 소모하지 않음
- 보통 턴은 동작 변화 없음 (`strict=false` 경로 그대로)
- 문법 오류 0

**실기기 확인 미완** (브라우저 확장 미연결): 실제 429 상황에서의 카운트다운 표시와 검색 완수,
그리고 5절의 비상 화법은 프롬프트 규칙이라 모델 순응도에 달려 있다. 첫 한도 상황에서 확인 권장.

---

## 8. 조정하는 법

| 원하는 것 | 방법 |
|---|---|
| 신선도 턴에도 2명 응답 | `FRESHNESS_RULES.soloResponder = false` |
| 신선도 턴 끼어들기 없애기 | `FRESHNESS_RULES.chainChance = 0` |
| 더 오래 기다려서라도 검색 완수 | `FRESHNESS_RULES.waitCapMs` 를 올린다 (기본 90000) |
| 기다리지 말고 바로 답하기 | `FRESHNESS_RULES.waitCapMs = 0` |
| 재시도를 더 자주 | `RETRY_RULES[429].baseMs` 를 낮춘다 (권장하지 않음) |
