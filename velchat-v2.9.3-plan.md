# 벨챗 v2.9.3 계획서 — 안정화 + 측정 + 데이터 계층

작성일 2026-08-17 · 대상 `vela-boardroom-prototype.html`
**상태: 계획만. 승인 전까지 코드 변경 없음.**

외부 리뷰 결론에 동의한다: 병목은 무료 한도 자체가 아니라 **메시지당 LLM 작업량**과
**실시간 정보를 LLM 검색에 의존하는 구조**다. 순서도 그대로 따른다 —
무한대기 → 호출량 → 구조화 데이터 → 캐시/검증 → 측정 → 유료.

---

## 0. 조사 결과 요약 (실측)

| 항목 | 결과 |
|---|---|
| 무한 대기 원인 | **찾았다.** `resolveRoute`가 쿨다운 중인 provider도 반환한다 (아래 1-1) |
| frankfurter.dev | 무키 · `access-control-allow-origin: *` · `v1/latest` 동작 확인 |
| frankfurter 실측 | `{"base":"USD","date":"2026-08-14","rates":{"KRW":1411.27}}` — **오늘(8/17) 조회인데 날짜가 8/14** |
| open-meteo | 무키 · `access-control-allow-origin: *` · `current=temperature_2m,weather_code` 동작 |
| open-meteo 실측 | 서울 `{"temperature_2m":24.8,"weather_code":1,"time":"2026-08-17T20:00"}` |
| open-meteo 지오코딩 | **한글 검색 부정확** — "부산" → 위경도 36.38/128.37 (경북). 쓰지 않는다 |

---

## 1. [A] 무한 대기 제거 + 서킷 브레이커

### 1-1. "12초 뒤 찾아볼게요" 무한 반복 — 진짜 원인

v2.9.2의 대기 예산 로직은 정상이다. 문제는 **그 로직에 도달하지 못한다는 것**이다.

`resolveRoute` 의 auto 경로 마지막 세 줄:

```js
const ready = usable.filter(providerAvailable);
if (ready.length) return build(ready[0]);
if (!usable.length) return null;
const soonest = usable.slice().sort(...)[0];
return build(soonest);          // ← 쿨다운 중인데도 돌려준다
```

그래서 Gemini가 429로 쿨다운 중이어도 **route가 나온다.**
→ `!route && strict` 대기 경로로 안 가고 **바로 호출** → 또 429 →
catch의 strict 분기에서 12초 대기 → 다시 호출 → 또 429 → …

`guard < 8` 이므로 최대 8회 호출 + 7회 대기 ≈ **84초 + 호출 시간**.
사용자 눈에는 "12초 뒤 찾아볼게요"가 계속 반복되는 무한 루프로 보인다.
게다가 **그 8번의 호출이 전부 429를 한 건씩 더 생산한다** — 한도를 태우면서 기다린 셈이다.

### 1-2. 수정 (세 겹)

**① 예산 종료 경로 일원화**
대기 판단이 지금 두 군데(`!route && strict`, `catch` 안)에 흩어져 있고 계산식이 미묘하게 다르다.
`planFreshnessWait(state, hintMs)` 하나로 합쳐서 **한 곳에서만** 결정한다.
반환: `{ action: 'wait'|'giveup', ms, waitNo }`

**② 하드캡 5회**
`FRESHNESS_RULES.maxWaits = 5`. 예산(90초)과 **무관하게** 대기 횟수 자체를 자른다.
예산만으로는 3초씩 30번 기다리는 경우를 못 막는다.

**③ 쿨다운 중이면 strict에서는 물러선다**
`resolveRoute` 에 `strictSearch` 일 때는 `soonest` 폴백을 쓰지 않는다 → `null` 반환 →
대기 경로로 정상 진입. 이게 1-1의 직접 수정이다.

### 1-3. 서킷 브레이커 (신설)

```js
const CIRCUIT_RULES = {
  failThreshold: 3,      // 연속 이 횟수만큼 429/503 이면 OPEN
  openMs: 60000,         // OPEN 유지 시간 — 이 동안 아예 호출하지 않는다
  halfOpenProbe: true    // OPEN 이 풀리면 1회만 시험 호출, 실패하면 다시 OPEN
};
```

- 상태: `CLOSED`(정상) → 연속 실패 3회 → `OPEN`(호출 금지) → 60초 후 `HALF_OPEN`(1회 시험) → 성공하면 `CLOSED`, 실패하면 다시 `OPEN`
- 성공하면 연속 실패 카운터는 0으로.
- `resolveRoute` 는 OPEN 상태 provider를 **후보에서 완전히 제외**한다 (쿨다운보다 강하다).
- 검색 가능한 곳이 전부 OPEN이면 strict 대기가 **의미 없으므로 즉시 포기** → 비상 화법.
  이게 "기다렸는데 결국 안 되는" 최악의 경우를 없앤다.
- 라우팅 로그에 상태 표시: `⛔ Gemini 차단 중 (42초 남음)`

### 1-4. "그만 기다리기" 버튼

대기 중 말풍선 머리에 작은 버튼을 붙인다.

```
🔍 사용량이 많아 47초 뒤 찾아볼게요   [그만]
```

누르면 `rec.giveUpSearch = true` → 대기 루프가 즉시 빠져나와 검색 없이 답한다.
**취소(cancel)와 다르다** — 답은 받는다. 검색만 포기한다.

### 1-5. 새 메시지 시 대기 취소

`cancelStaleTurns` 가 이미 처리한다 (대기 중 턴은 `spoke=false` → abort).
`countdownSearchWait` 가 `rec.cancelled` 를 매 초 확인하므로 즉시 빠져나온다.
**추가 작업 없음 — 회귀 테스트로만 확인한다.**

---

## 2. [B] 측정 대시보드

### 2-1. 수집 항목 (일자별)

```js
const METRICS = {
  enabled: true,
  keepDays: 14,          // localStorage 에 보관할 일수
  buckets: [250, 500, 1000, 2000, 4000, 8000, 16000, 32000]  // 응답시간 히스토그램(ms)
};
```

| 지표 | 세는 곳 |
|---|---|
| `messages` | `sendChatMessage` 진입 |
| `calls` | `callLLM` 성공·실패 모두 |
| **`callsPerMessage`** | `calls / messages` — **핵심 KPI** |
| `searchCalls` | `o.webSearch` 가 붙어 나간 호출 |
| `dataCalls` | 데이터 API 직결 호출 (3절) |
| `e429` / `e503` | 상태코드별 |
| `retries` | 대기 후 재시도한 횟수 |
| `circuitOpens` | 서킷이 열린 횟수 |
| `avgMs` / `p95Ms` | 히스토그램 버킷으로 근사 |

**P95를 버킷으로 근사하는 이유**: 응답시간 원본을 다 저장하면 localStorage가 커진다.
버킷 9칸 카운터면 하루치가 100바이트 남짓이다. 14일 보관해도 2KB 이하.

### 2-2. 화면

설정 하단에 접이식 "📊 측정" 패널. 오늘 + 최근 7일 요약.

```
오늘 (8/17)
  메시지 24 · LLM 호출 31 · 메시지당 1.29 ⚠      ← 목표 1.0 이하
  검색 6 · 데이터API 4 · 429 2 · 503 1 · 재시도 3
  응답 평균 2.1초 · P95 6.4초
  서킷 열림 1회
```

`callsPerMessage` 는 **1.0 이하면 초록, 1.5 초과면 경고색.**
목표를 화면에 박아둬야 개선이 눈에 보인다.

### 2-3. 목표

**CALLS_PER_MESSAGE 1.0 이하.** 현재 추정 1.15(신선도 턴) ~ 2.3(일반 안건).
데이터 계층(3절)이 붙으면 환율·날씨는 **LLM 검색 0콜 + 응답 1콜 = 정확히 1.0** 이 된다.

---

## 3. [C] 데이터 계층 — API → Fact → LLM → Validator

### 3-1. 구조

```
사용자: "환율 얼마야?"
   ↓
① 신선도 판정 → kind='fx'
   ↓
② DATA_SOURCES['fx'] 호출 (frankfurter, 무키·CORS OK)
   ↓
③ Fact 객체로 정규화
   { kind:'fx', value:1411.27, unit:'원', label:'미국 달러 1USD',
     source:'ECB 기준환율(frankfurter.dev)', asOf:'2026-08-14',
     caveat:'국제 기준환율이라 은행 환전가와 다릅니다' }
   ↓
④ 페르소나 프롬프트에 주입 — "이 값을 바꾸지 말고 설명만 해라"
   ↓
⑤ 응답 생성 (LLM 1콜, 검색 없음)
   ↓
⑥ Validator — 응답 속 숫자가 Fact와 일치하나?
   불일치 → 1회 재생성 → 또 불일치 → 앱이 값을 교정하고 표시
```

**핵심 이득**: 환율·날씨는 LLM 검색이 아예 필요 없어진다. 검색 호출 0, 지연 0,
그리고 **숫자가 출처와 일치함을 앱이 보증한다.**

### 3-2. `DATA_SOURCES` CONFIG (소스 추가 = 한 항목)

```js
const DATA_SOURCES = {
  fx: {
    label: '환율',
    match: ['환율', '달러', '엔화', '유로', '원달러'],   // freshness 카테고리와 별개로 세밀하게
    url: (q) => `https://api.frankfurter.dev/v1/latest?base=${q.base}&symbols=KRW`,
    parse: (json, q) => ({ ... }),
    ttlMs: 30 * 60 * 1000,
    caveat: '국제 기준환율(ECB 계열)이라 은행 환전가와 다를 수 있어요'
  },
  weather: { ... }
};
```

### 3-3. 환율 — 반드시 붙일 라벨

실측에서 **오늘(8/17) 조회인데 응답 날짜가 8/14** 였다.
ECB 계열 기준환율은 영업일 하루 한 번 갱신이라 주말·공휴일에는 며칠 전 값이 나온다.
그리고 실거래가(사용자가 본 1415원)와도 차이가 난다.

→ **`asOf` 를 프롬프트와 화면 양쪽에 반드시 노출한다.**

```
1 USD = 1,411.27원
국제 기준환율(ECB 계열) · 2026-08-14 기준 · 은행 환전가와 다를 수 있어요
```

이 라벨이 없으면 v2.9.1에서 고쳤던 문제(시점 없는 숫자가 현재값으로 읽히는 것)가
**데이터 API를 쓰면서 그대로 재발한다.** 소스가 정확해도 표기가 없으면 같은 사고다.

### 3-4. 날씨 — 도시 좌표는 내장한다

open-meteo 지오코딩은 한글 검색이 부정확했다 (`부산` → 경북 좌표).
그래서 **지오코딩 API를 쓰지 않고** 주요 도시 좌표를 CONFIG에 내장한다.
왕복도 하나 줄고 정확도도 올라간다.

```js
const KO_CITIES = {
  서울: [37.5665, 126.9780], 부산: [35.1796, 129.0756], 대구: [35.8714, 128.6014],
  인천: [37.4563, 126.7052], 광주: [35.1595, 126.8526], 대전: [36.3504, 127.3845],
  울산: [35.5384, 129.3114], 세종: [36.4800, 127.2890], 제주: [33.4996, 126.5312],
  수원: [37.2636, 127.0286], 성남: [37.4200, 127.1265], 고양: [37.6584, 126.8320],
  용인: [37.2411, 127.1776], 창원: [35.2280, 128.6811], 청주: [36.6424, 127.4890],
  전주: [35.8242, 127.1480], 천안: [36.8151, 127.1139], 춘천: [37.8813, 127.7300],
  강릉: [37.7519, 128.8761], 포항: [36.0190, 129.3435]
};
// 도시명이 없으면 기본 서울. 확신 없으면 데이터 계층을 건너뛰고 LLM 검색으로 넘긴다.
```

WMO `weather_code` 는 한국어 표로 변환한다 (0 맑음 / 1~3 대체로 맑음~흐림 / 45·48 안개 /
51~67 이슬비·비 / 71~86 눈 / 95~99 뇌우).

### 3-5. 캐시

`ttlMs` 동안 같은 질의는 재호출하지 않는다 (환율 30분, 날씨 10분).
`localStorage` 가 아니라 **메모리 캐시**로 둔다 — 새로고침하면 다시 받는 게 안전하다.

### 3-6. Validator (6번)

**검사 방법**
- Fact 종류별 숫자 추출 정규식: `fx` → `/([\d,]+(?:\.\d+)?)\s*원/g`, `weather` → `/(-?[\d.]+)\s*도/g`
- 추출된 숫자 중 Fact 값과 **상대오차 20% 이내**인 것들만 "이 값을 말하려는 시도"로 본다.
  (20% 밖은 "작년엔 1,300원이었죠" 같은 과거 언급이므로 건드리지 않는다 — 오탐 방지)
- 그중 하나라도 허용 오차(`fx` 0.5%, `weather` 0.5도) 밖이면 **불일치**.

**불일치 처리**
1. 1회 재생성 — 같은 Fact + `"숫자는 정확히 1,411.27원으로 써라"` 를 덧붙여 재요청
2. 그래도 불일치 → **앱이 그 숫자 토큰만 정확한 값으로 치환**하고 말풍선 하단에 표시:
   `※ 숫자는 출처 기준으로 정정했습니다`

**리스크와 완화**: 치환은 문맥을 모르므로 위험하다.
→ 20% 창 밖은 손대지 않고, 치환한 사실을 반드시 화면에 남기며,
→ 치환 횟수를 측정 지표(`corrections`)로 세서 과하면 규칙을 되돌릴 수 있게 한다.

### 3-7. 폴백 순서 (7번)

```
데이터 API 성공 → Fact 주입, 검색 없음 (LLM 1콜)
데이터 API 실패 → LLM 웹 검색 (기존 v2.9.2 경로)
검색도 실패    → 비상 화법 (아래 4절)
```

데이터 API는 타임아웃 4초. 넘으면 기다리지 않고 검색으로 넘어간다 —
"빠르려고 붙인 계층"이 느림의 원인이 되면 안 된다.

---

## 4. [D] 화법 — 못 지킬 약속 금지

v2.9.2에서 "확인해볼게요" 류가 남아 있다. **이 방의 페르소나는 나중에 혼자 확인할 수단이 없다.**
사용자가 다시 묻지 않으면 영영 안 온다. 지키지 못할 약속은 거짓말과 같다.

| 금지 | 대체 |
|---|---|
| "확인해볼게요" | "잠시 뒤에 다시 물어봐 주시면 찾아볼게요" |
| "알아보고 알려드릴게요" | "지금은 확인이 잘 안 되네요. 다시 한번 물어봐 주세요" |
| "잠시만요, 찾아볼게요" | "지금 숫자는 확인이 필요해요" |
| "이따 알려드릴게요" | "조금 뒤에 다시 여쭤보시면 그때 찾아볼게요" |
| "곧 업데이트해서" | (그냥 쓰지 않는다) |

규칙 한 줄로 요약해 `COMMON_RULES` 에 넣는다:
> **네가 나중에 혼자 할 수 있는 일은 없다.** 다음 행동은 항상 {chair}가 다시 물어보는 것이다.

---

## 5. (조사만) 공유 분석 구조 — v3.0 실험 플래그 후보

### 5-1. 제안된 안의 문제

"분석 1콜 → 2명이 관점만 짧게" 를 그대로 구현하면:

```
분석 1콜 + 발언 2콜 = 3콜   (현재 2콜)
```

**KPI가 오히려 나빠진다.** 토큰은 줄어도 `CALLS_PER_MESSAGE` 는 1.15 → 1.5로 역행한다.
호출 수가 무료 한도의 단위이므로 이 방향은 지금 목표와 충돌한다.

### 5-2. 대안 — 한 콜에서 두 사람을 함께 생성

```
1콜 → JSON { analysis, turns: [{persona:'analyst', text}, {persona:'care', text}] }
     → 앱이 말풍선 2개로 분리해 시간차를 두고 렌더
```

- 호출 **1회**. `CALLS_PER_MESSAGE` 목표(1.0)에 정확히 부합한다.
- 스트리밍과 궁합: 첫 번째 사람 것만 스트리밍하고 두 번째는 완성 후 붙이거나,
  JSON 스트리밍 파서를 얕게 만들어 순차 렌더.

### 5-3. 개성 유지 리스크 (정직하게)

한 번의 생성으로 두 사람을 쓰면 **말투가 서로 수렴한다.** 이게 가장 큰 위험이고,
5인 체계의 핵심 가치를 정면으로 깎는다. 완화책 셋:

1. 각 인물의 `samples` 3개를 **둘 다** 프롬프트에 넣고 "두 사람은 완전히 다른 사람이다" 명시
2. 생성 후 **말투 검사** — 이모지·어미·문장 길이가 그 인물의 프로필과 어긋나면 재생성
3. **A/B 플래그로 켜고**, 측정 패널에 `callsPerMessage` 와 나란히
   "말투 재생성률"을 세서 품질 저하를 수치로 감시

→ **결론: v2.9.3에서는 구현하지 않는다.** `FEATURES.sharedTurn = false` 자리만 만들어두고,
측정 기반이 갖춰진 뒤(2절) v3.0에서 A/B로 검증하는 게 순서다.

---

## 6. 구현 순서 · 회귀 확인

1. 서킷 브레이커 + `resolveRoute` strict 폴백 제거 (1-1의 직접 수정)
2. 대기 경로 일원화 + 하드캡 5회 + "그만" 버튼
3. 측정 계층 (이후 작업의 효과를 숫자로 보기 위해 먼저 넣는다)
4. `DATA_SOURCES` + Fact + 프롬프트 주입
5. Validator + 교정 표기
6. 화법 규칙
7. `FEATURES.sharedTurn` 자리만 추가 (기본 false, 미구현)
8. v2.9.3 스탬프 · 커밋 · 푸시

**회귀 확인 목록**
- 보통 턴(비신선도) 동작 무변화
- 신선도 턴: 데이터 API 성공 / 실패→검색 / 검색도 실패→비상 화법 3경로
- 서킷 OPEN 중 다른 provider로 즉시 전환
- 새 메시지로 대기 취소
- 옛 세션(정민철 포함) 정상 표시 — v2.9 회귀 방지
- 문법 검사 + 라우팅 엔진 헤드리스

---

## 7. 리스크

| 리스크 | 대응 |
|---|---|
| ECB 환율이 며칠 전 값 | `asOf` 라벨 필수 (3-3). 이걸 빠뜨리면 데이터 API를 붙이고도 같은 사고 |
| 실거래가와 차이 | `caveat` 로 "은행 환전가와 다름" 항상 표기 |
| Validator 오탐으로 멀쩡한 숫자 치환 | 20% 창 밖 무시 + 치환 표시 + `corrections` 지표 감시 |
| 데이터 API 장애 | 4초 타임아웃 → 검색 폴백 → 비상 화법 (3단) |
| 서킷이 너무 자주 열림 | `failThreshold`·`openMs` CONFIG. 측정 패널의 `circuitOpens` 로 감시 |
| 측정 저장소 증가 | 버킷 카운터만 저장, 14일 순환 (2KB 이하) |
| 무키 API 정책 변경 | `DATA_SOURCES` 한 항목 교체로 대응 가능한 구조 |
