# Vela Compass v2.3.0 — 상세 변경 노트

> 2026-08-14 · 대상 파일: `vela-boardroom-prototype.html` (단일 파일 앱)
> 이전 버전: v2.2.0 (입력창 고정 · 사진 업로드 · 유머 톤 · 깨진 글자 필터)

이번 버전의 주제는 **"네 사람이 각각 다른 한국인처럼 말하게 만들기"** 다.
v2.2 가 문자 깨짐을 잡았다면, v2.3 은 영어 혼입과 말투 획일화를 잡는다.

---

## 1. 영어 혼입 차단 — 필터가 아니라 재생성으로

### 왜 필터로는 못 잡나
v2.2 의 `ALIEN_CHAR_RE` 는 **영문자를 허용 문자로 둔다.** API·KPI 같은 약어와
브랜드명을 살려야 하기 때문이다. 그래서 아랍어·키릴 문자는 지울 수 있어도
`"Let me break this down."` 같은 **영어 문장**은 잡을 수 없다.
지우면 문장 자체가 사라져 버린다.

→ 문자 단위로 접근하는 대신, **"이 답변이 한국어인가"를 비율로 재고
모델에게 다시 쓰게 시킨다.**

### 1-A. 프롬프트 강화 — `COMMON_RULES` 에 영어 전용 섹션 신설

```
[영어 — 단어만 허용, 문장은 금지]
```

- **모든 문장은 한국어.** 영어 문장·구절 금지.
- 영어를 쓸 수 있는 경우는 딱 둘:
  ① 기술·업계 약어 (API, MES, ERP, KPI, CS, SKU, QC, CAD, SaaS)
  ② 브랜드·기관·제품·인명 고유명사
- 나머지 영어 단어는 전부 한국어로 — `process→공정`, `workflow→작업 흐름`,
  `target→목표`, `insight→시사점`, `cost→원가`, `risk→위험`, `timeline→일정`
- **금지 예시를 명시적으로 나열**했다 (모델은 추상 규칙보다 반례에 반응한다):
  `"Let me break this down." / "Here's the thing:" / "In short," / "That said,"`
  → `"정리해보면" / "핵심은 이겁니다" / "요약하면" / "다만"`
- 혼합 문장도 금지: `"그 approach는 risk가 too high합니다"` → `"그 방식은 위험이 너무 큽니다."`
- 소제목도 한국어: `Pros/Cons`, `Summary`, `Next steps` → `장점/단점`, `정리`, `다음 할 일`

### 1-B. 한글 비율 검사 + 1회 자동 재생성

```js
const LANG_GUARD = {
  minLength: 20,        // 이보다 짧은 답변은 검사하지 않는다
  minLetters: 10,       // 판정에 쓸 글자가 이보다 적으면 통과
  minHangulRatio: 0.5,  // 한글 / (한글+영문) 하한
  retries: 1
};
```

**비율 계산 (`hangulRatio`)** — 분모는 "글자"만 센다.

- 분자: 한글 음절 + 자모 (`가-힣`, `ㄱ-ㅎ`, `ㅏ-ㅣ`)
- 분모: 한글 + 영문 알파벳
- **제외**: 숫자·기호·이모지·공백 (언어 판정과 무관 — `1,200원` 이 비율을 흐리면 안 된다)
- **제외**: URL · 마크다운 링크/이미지 · 인라인 코드
  (영문일 수밖에 없는 부분이라 오탐의 주범)

**동작 흐름** (`runPersonaTurn`)

```
callLLM → SVG 분리 → sanitizeKorean → 이름표 제거
   ↓
isTooMuchEnglish(text) ?
   ├ 아니오 → 그대로 표시 (추가 콜 없음)
   └ 예 → logKoreanRatio('detected')
           ↓ assistant(원문) + user(재작성 지시) 를 붙여 1회 재호출
           ↓ 재생성이 "더 나을 때만" 채택 (한글 비율 비교 — 더 나빠지는 경우도 있다)
           ↓ logKoreanRatio('recovered' | 'gaveup')
```

- 재생성 지시문(`KOREAN_REWRITE_DIRECTIVE`)은 **같은 내용을 처음부터 다시**
  쓰게 한다. 약어·고유명사는 남기고, 말투와 길이는 유지하고,
  사과나 설명 없이 본문만 출력하게 한다.
- 재생성 호출 자체가 실패하면(네트워크·한도) 원본을 그대로 쓰고 경고만 남긴다.
- **재생성해도 기준 미달이면 그대로 표시한다.** 사용자에게 빈 화면을 주지 않는다.
  다만 `gaveup` 으로 반드시 기록을 남긴다.

**추가 비용**: 영어가 섞였을 때만 1콜 추가. 정상 답변은 콜이 늘지 않는다 (검증됨).

### 1-C. 추적 로그

`window.__compassLangLog` (최근 50건) + `console.warn`

기록 항목: `provider` · `model` · `persona` · `stage` · `hangulRatio` ·
한글/영문 글자 수 · 답변 원문 · 타임스탬프

```
[compass:한국어] 영어 혼입 감지 — 재생성 시도 · 한글비율 0% (한글 0 / 영문 52)
                 — anthropic/claude-sonnet-4-6 · strategist
[compass:한국어] 재생성으로 해결 · 한글비율 100% (한글 27 / 영문 0)
                 — anthropic/claude-sonnet-4-6 · strategist
```

`stage` 값 세 가지로 어느 모델이 얼마나 자주 영어를 섞는지, 재생성으로
회복되는지까지 구분된다:
- `detected` — 영어 혼입 감지
- `recovered` — 재생성으로 해결
- `gaveup` — 재생성해도 실패 (그대로 표시됨)

v2.2 의 문자 필터 로그(`window.__compassFilterLog`)와는 별개 배열이다.
문자 깨짐과 영어 혼입은 원인이 다른 문제라 섞지 않았다.

---

## 2. 페르소나 말투 전면 차별화

각 페르소나에 `voice`(말투) · `humor`(개그) · `samples`(예시 대사 3개)를
추가하고, `personaVoiceBlock()` 이 하나의 블록으로 조립한다.
(v2.2 의 `humorStyleBlock()` 을 대체)

**예시 대사(few-shot)가 말투 고정에 가장 크게 작용한다.**
규칙 열 줄보다 대사 한 줄이 낫다 — 모델은 설명보다 형태를 모방한다.

### 서다현 — 텐션 높음
- 감탄사로 시작: `오!!`, `헐`, `미쳤다`, `아 잠깐만`, `이거다`
- 느낌표·물음표를 아끼지 않고 말끝을 올린다 — `~인데?`, `~하지 않아?`, `~거든!`
- 이모지 자주 (✨🔥👀💡😂) · 한 발언에 한두 개
- 흥분하면 존댓말에 반말이 섞여 나온다
- 개그: 엉뚱한 비유 드립

> "오!! 잠깐 이거 완전 물건인데?? 👀 근데 이름이 너무 착해요. 좀 더 뻔뻔한 이름이어야 눈에 박혀요."

### 이준호 — 짧고 툭툭
- 문장이 짧고 수식어가 없다. 끝나면 그냥 끝난다.
- 체념 어미: `~인데요 뭐`, `~하긴 하죠`, `뭐 그렇습니다`, `안 되죠 그건`
- 밋밋한 감탄사: `아`, `음`, `그쵸`, `허` · 이모지 거의 안 씀
- 먼저 김을 빼고 시작하되, 끝에는 항상 할 수 있는 걸 하나 남긴다
- 개그: 현실 자학개그

> "해보면 압니다. 저도 그거 하다가 3주 날렸는데요 뭐ㅋㅋ 샘플 두 개만 먼저 만들어보시죠."

### 정민철 — 문장 정갈
- 항상 존댓말, 흥분해도 안 무너진다. `~입니다`, `~죠`, `~습니다`
- 감탄사·ㅋㅋ·이모지를 쓰지 않는다. 마침표로 끊는다.
- 숫자를 문장에 자연스럽게 박는다 — `월 30개`, `마진 22%`, `회수까지 14개월`
- 남들이 신나 있을 때 온도를 한 칸 내리되, 조건을 같이 준다
- 개그: 건조한 팩트 개그. 웃길 때조차 진지한 표정.

> "좋은 아이디어입니다. 월 3개 팔면 회수까지 12년이 걸린다는 점만 빼면요."

### 전문가석 — 업계 은어 (동적 배정이라 패턴으로 지시)
- 실무자끼리만 쓰는 은어·현장 용어·줄임말을 섞는다
- 필요할 때만 짧게 풀어준다 — "현장에선 ○○라고 하는데, 쉽게 말하면 △△죠"
- 교과서가 아니라 겪은 걸로 말한다
- 예시는 제조 쪽 말투로 주되, **`{expertTitle}` 로 치환해 자기 분야로 옮기라고 지시**한다
  (배정된 전문가가 매번 달라지므로 고정 예시를 쓸 수 없다)

> "그거 현장에선 그렇게 안 굴러갑니다. 도면상으론 완벽하죠. 도면상으론ㅋㅋ"
> → *"위 예시는 제조 쪽 말투다. 너는 공정 엔지니어 분야의 은어와 현장 용어로 똑같이 하면 된다."*

### 말투 유지 장치
각 블록 끝에 공통으로 붙는다:

> 네 사람은 말투가 완전히 다르다. 화면에 이름이 없어도 누가 말했는지 알아볼 수 있어야 한다.
> 남의 말투를 흉내 내지 마라. 특히 다른 사람의 감탄사·어미·이모지 습관을 가져오지 마라.

예시 대사 뒤에도: *"그대로 베끼지 마라. 이 톤·리듬·어미·길이를 유지하라는 뜻이다."*

---

## 3. 유머 빈도 상향 — "가끔"에서 "기본"으로

`HUMOR_RULES` 의 프레이밍을 뒤집었다.

**전(v2.2)**: "웃겨도 된다 — 오히려 권장" + *"매 발언마다 개그 넣으면 피곤하다"*
**후(v2.3)**: "가벼움이 기본값이다 — 이 방의 기본 온도"

- **가벼운 게 정상이고, 진지한 게 예외다.**
- 잡담·가벼운 대화에서는 유머를 섞는 게 **기본**. 정보만 던지면 오히려 어색하다.
  → *"'가끔'이 아니라 '웬만하면'이다."*
- **무게에 따라 온도가 다르다**: 가벼운 얘기일수록 더 웃기고, 무거운 얘기일수록 절제.
- 동료 이름을 부르며 놀리는 게 방을 가장 살린다 (티키타카 강화)
- 억지로 짜내지는 마라 — 안 떠오르면 말투만 살려도 충분하다
- 사용자 비하 금지 · 속성(외모·출신·성별·나이·장애) 소재 금지는 그대로 유지

`MESSAGE_WEIGHTS` 의 무게별 지시문에도 반영했다:
- `casual` → "여기선 유머가 기본이다"
- `normal` → "딱딱하게 가지 마라 — 네 말투와 유머를 살려서"
- `serious` → "유머를 절제해라. 본론이 개그에 묻히면 안 된다"

---

## 4. 답변 길이 추가 하향

카톡답게 더 짧게. `MESSAGE_WEIGHTS` 와 `COMMON_RULES` 양쪽에서 조인다.

- **리액션(filler)**: "**딱 한 줄.** 여기서 길게 쓰면 실패다."
- **잡담(casual)**: "**1~2문장이 기본. 3문장 넘어가면 실패.** 카톡에서 친구한테 답하듯 툭."
- **대화(normal)**: 2~3문장 (유지)
- **안건(serious)**: 3~5문장 (유지)

`COMMON_RULES` 의 길이 규칙도 구체화:

> 잡담이면 1~2문장이 기본이고, 리액션이면 한 줄이다.
> 3문장이 넘어가는 순간 "이 사람 왜 이렇게 길게 쓰지?" 소리를 듣는다.

---

## 검증 (실제 브라우저 실측)

### 한글 비율 판정 — 8개 케이스 전부 기대대로

| 입력 | 한글비율 | 결과 |
|---|---|---|
| 순수 한국어 + 숫자 (`원가 1,200원 마진 22%`) | 100% | 통과 |
| `Let me break this down. The key point is...` | 0% | **차단** |
| `그 approach는 risk가 too high합니다...` | 11% | **차단** |
| `Here is the summary: ... 원가 절감이 핵심입니다.` | 23% | **차단** |
| `ㅇㅇ 콜` (4자) | — | 검사 안 함 (짧음) |
| `API 확인해볼게요` (10자) | — | 검사 안 함 (짧음) |
| `MES랑 ERP 연동은 API로... QC 쪽 KPI도` | 59% | 통과 (약어 다수) |
| 긴 영문 URL 포함 한국어 문장 | 100% | 통과 (URL 제외) |

기술 약어가 많은 정상 한국어(59%)는 통과하고 영어 문장은 차단된다 — 임계값 50%가 적절히 갈라낸다.

### 재생성 흐름
- **복구**: 영어 응답 → 재생성 요청 발송 확인 → 한국어로 교체 → `detected`+`recovered` 로그
- **포기**: 재생성도 영어 → 그대로 표시 + `gaveup` 로그 (provider/model = groq/llama-3.1-8b 식별됨)
- **정상**: 한국어 응답 → **호출 1회, 로그 0** (불필요한 재생성 없음)

### 페르소나
- 4인 전원 말투·개그·예시 3개 섹션 주입 확인
- 전문가석 동적 배정(한서준 / 공정 엔지니어) 후 `{expertTitle}` 치환 정상
- 템플릿 토큰(`{chair}` `{topic}` `{expertTitle}` 등) 미치환 잔여 0건
- 프롬프트 길이: 페르소나당 약 4,950자 (≈ 2,260 토큰). v2.2 대비 증가분은
  예시 대사와 영어 규칙 때문이다. 1콜당 **1인분만** 전송되므로 콜당 비용 영향은 제한적.

### v2.2 회귀 — 이상 없음
고정 셸 레이아웃 · 메시지 영역만 스크롤 · 문자 필터 · 스크롤 정책(강제 스크롤
금지 + 배지) · 사진 첨부 · 멀티모달 이미지 블록 · Groq 이미지 제거 전부 정상.

---

## 변경 파일

- `vela-boardroom-prototype.html` — 전부 여기 (단일 파일 앱)
- `vela-boardroom-sw.js` — 스탬프가 BUILD_ID 갱신
- `vela-compass-v2.3.0-notes.md` — 이 문서 (신규)

## 유지보수 메모

v2.3 에서 추가·변경된 CONFIG 항목:

- `LANG_GUARD` — 한글 비율 하한 · 검사 최소 길이 · 재생성 횟수
- `CORE_PERSONAS[].voice` / `.samples` — 말투와 예시 대사 (새 인물 추가 시 이 둘만 쓰면 된다)
- `EXPERT_VOICE` / `EXPERT_SAMPLES` — 전문가석 말투
- `HUMOR_RULES` — 유머 빈도 프레이밍
- `MESSAGE_WEIGHTS[].tone` — 무게별 길이·유머 지시

**말투를 바꾸고 싶으면 `samples` 를 먼저 고쳐라.** 규칙 문장을 손대는 것보다
예시 대사 세 줄을 바꾸는 쪽이 훨씬 크게 작동한다.

v2.0 회의 기능(라운드·회의록·모드 선택)은 `FEATURES` 플래그로 여전히 꺼둔 상태이며
코드는 그대로 남아 있다.
