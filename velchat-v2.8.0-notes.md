# 벨챗 v2.8.0 — Gemini · 대화 중 입력 · 응답 속도 · 출처 규칙

네 가지가 한 버전에 묶였지만 사실 하나의 목표를 향한다.
**"단톡방처럼 느껴지게"** — 빨리 답이 뜨고, 아무 때나 말을 걸 수 있고, 거짓말을 안 하는 것.

---

## 1. Google Gemini provider

### 왜 Gemini 인가

기존 둘의 빈칸을 정확히 메운다.

- Anthropic — 한국어 품질 최고, 사진 봄, **유료**
- Groq — 무료, 빠름, **사진 못 봄**
- **Gemini — 무료 티어 + 사진 인식 + Flash 계열은 가장 빠름**

"무료인데 사진도 보는" 조합은 지금까지 없었다.

### 형식이 다른 유일한 provider

Anthropic·Groq 은 둘 다 `POST /엔드포인트` 에 `messages[]` 를 싣는다.
Gemini 는 **모델 이름이 URL 경로에 들어가고**, 스트리밍이면 동사까지 바뀐다.

```
POST .../v1beta/models/gemini-2.5-flash:generateContent
POST .../v1beta/models/gemini-2.5-flash:streamGenerateContent?alt=sse
```

그래서 `cfg.endpoint` 문자열 하나로는 안 되고, 설정에 함수를 하나 뒀다.

```js
endpointFor: function (model, opts) {
  const m = encodeURIComponent(model || 'gemini-2.5-flash');
  const verb = (opts && opts.stream) ? 'streamGenerateContent?alt=sse' : 'generateContent';
  return `https://generativelanguage.googleapis.com/v1beta/models/${m}:${verb}`;
}
```

다른 provider 는 `endpointFor` 가 없으므로 `providerEndpoint()` 가 `cfg.endpoint` 로 떨어진다.
**Gemini 하나 때문에 나머지 둘의 코드가 바뀌지 않는다** — 이게 이 설계의 요점이다.

### 키를 URL 에 싣지 않는다

Gemini 문서는 `?key=API_KEY` 를 먼저 보여주지만, 그 방식은 키가
브라우저 히스토리·프록시 로그·리퍼러 헤더에 그대로 남는다.
`x-goog-api-key` 헤더를 쓴다. 기능은 같고 흔적이 안 남는다.

```js
function providerHeaders(provider, key) {
  if (provider === 'anthropic') return { 'x-api-key': key, 'anthropic-version': '2023-06-01', ... };
  if (provider === 'gemini')    return { 'x-goog-api-key': key, 'content-type': 'application/json' };
  return { 'Authorization': 'Bearer ' + key, 'content-type': 'application/json' };
}
```

세 곳(본 호출·스트림·연결 테스트)이 이 함수 하나를 공유한다.
예전에는 `testApiKey()` 가 헤더를 따로 만들고 있어서, 본 호출과 테스트가 어긋날 여지가 있었다.

### 이미지 — 기존 변환 계층에 한 줄

내부 표준은 Anthropic 블록 형식이다. Gemini 는 `contents/parts` 라 통째로 다르다.

```js
function normalizeMessagesForProvider(messages, provider) {
  if (provider === 'gemini') return messages;   // 손대지 않고 넘긴다
  ...
}
```

그리고 요청 직전에 `toGeminiContents()` 가 한 번에 바꾼다.

- `role: assistant` → `model`
- 문자열 content → `parts: [{text}]`
- `{type:'image', source:{media_type, data}}` → `{inline_data: {mime_type, data}}`

**여기서 테스트가 실제 버그를 하나 잡았다.** 빈 문자열 content 를 그대로 넘기면
Gemini 가 `INVALID_ARGUMENT` 로 거절한다. Anthropic·Groq 은 조용히 넘어가서
Gemini 에서만 터지는 종류의 사고다. 빈 파트를 버리도록 고쳤다.

```js
const t = String(m.content == null ? '' : m.content);
parts = t.length ? [{ text: t }] : [];
```

또 하나 — Gemini 는 첫 메시지가 `model` 이면 거부한다.
그럴 때 앞에 빈 user 턴을 세운다 (Anthropic 히스토리 병합 로직과 같은 대비책).

### 연결 테스트 — Gemini 는 HTTP 코드가 뭉툭하다

Anthropic·Groq 은 키가 틀리면 401 을 준다. **Gemini 는 400 을 준다.**
모델 이름이 틀려도 400 이고, 형식이 틀려도 400 이다.
그래서 HTTP 코드가 아니라 **본문의 `error.status` 문자열**로 갈라야 한다.

```js
if (/API_KEY_INVALID|API key not valid/i.test(hay))  → 키 오타
if (403 || /PERMISSION_DENIED/)                       → 권한 (API 사용 설정)
if (429 || /RESOURCE_EXHAUSTED|quota/)                → 무료 티어 한도 (키는 유효)
if (404 || /NOT_FOUND|is not found for API version/)  → 모델 이름
if (503)                                              → 서버 혼잡 (키는 유효)
```

**순서가 중요하다.** 일반 분기의 `if (res.status === 404 || /model/i.test(raw))` 를
먼저 만나게 두면, Gemini 의 키 오타 메시지에 우연히 'model' 이 들어 있을 때
"모델 이름을 확인해주세요"로 잘못 안내한다. Gemini 전용 분기를 앞에 세운 이유다.

한도 초과와 서버 혼잡은 **`ok: true, warn: true`** 로 돌려준다 —
"키는 맞다, 지금 상황이 문제다"와 "키가 틀렸다"는 사용자가 할 일이 완전히 다르다.

네트워크에 아예 못 닿는 경우는 `fetch` 의 `catch` 에서 따로 잡는다.

### 키 형태 사전 검사

네트워크를 쓰기 전에 잡을 수 있는 오타가 제일 많다.

```js
if (cfg.keyPattern && !cfg.keyPattern.test(key)) {
  return { ok: false, msg: `${cfg.name} 키 형태가 아닙니다 — ${cfg.keyPlaceholder} 으로 시작해야 합니다` };
}
```

`keyPattern` 은 QR 스캔에도 재활용된다. 예전에는 `parseKeyPayload()` 안에
`/^sk-ant-/`, `/^gsk_/` 가 하드코딩돼 있어서 provider 를 추가할 때마다 두 곳을 고쳐야 했다.
이제 `LLM_CONFIG` 의 `keyPattern` 을 순회하므로 **한 곳만 고치면 된다.**

붙여넣기·보기 토글·연결 테스트·QR 전송/스캔이 전부 Gemini 에서 그대로 동작한다.

---

## 2. 대화 중에도 입력 — 카톡처럼

### 무엇이 문제였나

```js
async function sendFollowup() {
  if (state.inProgress) return;          // ← 응답 중엔 아예 안 받았다
  ...
  $('#btnFollowupSend').disabled = true; // ← 버튼도 잠갔다
```

세 명이 답하는 동안 10초 넘게 입력이 막힌다. 카톡에서는 상대가 타이핑 중이어도
내 입력창이 잠기지 않는다. **말을 거는 쪽이 기다려야 할 이유가 없다.**

### 세대 번호 하나로 푼다

```js
const seq = ++state.turnSeq;   // 사용자 메시지마다 +1
cancelStaleTurns(seq);
```

이후 모든 대기·표시 지점이 `seq !== state.turnSeq` 를 확인하고, 아니면 그 자리에서 멈춘다.
연속으로 세 개를 보내도 **항상 마지막 메시지 기준으로만** 답이 나간다.
라우팅(`pickResponders`)도 새 메시지로 다시 계산되므로, 주제가 바뀌면 답하는 사람도 바뀐다.

### 진행 중인 턴은 어떻게 하나 — 결정과 근거

요청서에서 "자연스러운 쪽으로 계획에 제시"라고 한 부분이다. 세 가지를 놓고 골랐다.

- **(A) 전부 취소** — 즉각적이지만, 말풍선이 문장 중간에서 잘려 사라진다. 버그처럼 보인다.
- **(B) 전부 완주** — 안전하지만, 이미 지난 주제에 대한 답이 새 메시지 뒤에 붙는다. 대화가 어긋난다.
- **(C) 말하기 시작했는지로 가른다** ← **채택**

사람이 말하다 마는 건 이상하지만, 입도 안 뗀 사람이 조용히 마음을 바꾸는 건 자연스럽다.
그 직관을 그대로 코드로 옮겼다.

```js
const INTERRUPT_RULES = {
  abortSilentTurns: true,   // 아직 글자가 안 나온 진행 턴은 중단
  keepSpokenTurns: true,    // 이미 말하기 시작한 턴은 끝까지
  noticeOnInterrupt: false  // 취소를 시스템 칩으로 알릴지 (기본: 조용히)
};
```

판정 기준은 `rec.spoke` 다 — **화면에 한 글자라도 나갔는가.**
"API 응답이 왔는가"가 아니라 "사용자가 봤는가"로 재는 게 핵심이다.
사용자가 못 본 건 없앨 수 있고, 본 건 없애면 안 된다.

취소는 `AbortController` 로 진짜 요청을 끊는다 — 무시하는 게 아니라 토큰을 안 쓴다.

기본은 조용히 취소한다. "답변 2건을 취소했습니다" 같은 칩은 정보 가치가 없고
방만 어지럽힌다. 디버깅이 필요하면 `noticeOnInterrupt: true` 로 켠다.

---

## 3. 응답 속도

### ① 스트리밍 — 기다림을 없애는 게 아니라 보이게 만든다

체감 속도의 대부분은 **첫 글자까지의 시간**이다.
전체가 3초 걸려도 0.4초에 첫 글자가 뜨면 빠르게 느껴진다.

`callLLM(..., { onDelta })` 로 조각을 흘린다. 세 provider 모두 SSE 를 쓰지만
델타의 위치가 다르다.

- Anthropic — `content_block_delta` 의 `delta.text_delta.text` (+ `citations_delta` 로 인용)
- Gemini — `candidates[0].content.parts[*].text`
- Groq — `choices[0].delta.content`

SSE 프레임 파서는 하나로 공유한다. **청크 경계가 프레임 한가운데를 자르는 경우**가
실제로 늘 일어나기 때문에, 버퍼에 모아두고 `\n\n`(또는 `\r\n\r\n`)이 나올 때만 잘라낸다.
깨진 JSON 프레임은 건너뛰고 계속한다. 이 세 가지를 전부 테스트로 고정했다.

**언어 가드는 스트림이 끝난 뒤에만 돈다.** 반쯤 온 문장으로 한글 비율을 재면
의미가 없고, 재생성 호출은 화면에 흘리지 않는다 —
흘리면 같은 말이 두 번 타이핑되는 것처럼 보인다.

```js
let result = await askOnce(messages, true);    // 첫 호출만 라이브
...
retried = await askOnce(retryMsgs, false);     // 재생성은 조용히
```

마크다운·SVG·인용도 스트림 중에는 그리지 않는다. 평문만 흘리고,
끝난 뒤 `renderBubbleContent()` 가 한 번에 제대로 그린다.
반쯤 온 마크다운을 그리면 화면이 깜빡이고 반쪽짜리 태그가 남는다.

스트림이 중간에 깨지면 **조용히 논스트리밍으로 한 번 더 간다.** 사용자는 모른다.

### ② 미리 걸기 — 두 번째 사람은 이미 답을 쓰고 있다

v2.7 까지는 완전 직렬이었다.

```
[1번 요청 ────] [지연] [2번 요청 ────] [지연] [3번 요청 ────]
```

이제 요청과 표시를 분리했다.

```
[1번 요청 ────]
[2번 요청 ──────────]      ← 1번이 말하는 동안 이미 진행 중
[3번 요청 ────────────────]
표시:  [1번 흐름] [지연] [2번] [지연] [3번]
```

화면에서 달라지는 건 **기다림이 없다**는 것뿐이다. 순서는 그대로다.

구조는 함수 두 개로 갈랐다.

- `startTurnRequest()` — 요청만 건다. 조각은 버퍼에 쌓인다. **화면은 안 건드린다.**
- `presentTurn()` — 버퍼를 화면에 흘리고, 끝나면 언어 가드를 걸어 확정한다.

**표시기가 "실시간"과 "미리 받아둔 것"을 같은 코드로 처리한다.** 프레임마다
남은 글자의 1/12씩(최소 2자) 밀어 올린다.

```js
shown = Math.min(target.length, shown + Math.max(2, Math.ceil((target.length - shown) / 12)));
```

- 라이브 → 남은 게 늘 적어 최소 속도(약 120자/초)로 자연히 따라간다
- 미리 받아둔 것 → 남은 게 크니 처음엔 빠르게, 끝에선 부드럽게 수렴한다

측정해보니 300자를 39프레임(60fps 기준 0.65초), 1200자를 55프레임에 따라잡는다.
분기 없이 한 공식으로 둘 다 된다.

#### 미리 걸면 동료 말을 못 본다 — 이건 숨기지 않고 알려준다

2번은 1번이 뭐라 했는지 모르는 상태로 답을 쓴다. 안 본 말을 본 척하면 대화가 어긋난다.
그래서 프롬프트에 사실대로 적는다.

> `{chair}의 말에 너도 답한다. 동료 ○○도 지금 동시에 쓰고 있어서 그 사람 말은 아직 못 봤다.`
> `동료 말을 인용하거나 "○○ 말처럼" 같은 식으로 받아치지 마라 — 아직 못 봤으니까.`

단톡방에서 두 사람이 동시에 타이핑하는 상황과 정확히 같다. 오히려 더 자연스럽다.

**연쇄 반응(끼어들기)만 순차로 남겼다.** 반응할 대상이 아직 없으니 미리 걸 수가 없다.
속도보다 앞뒤가 중요한 유일한 자리다.

**Groq 에서는 미리 걸지 않는다.** 분당 요청 제한이 빡빡해서 동시에 쏘면 429 가 난다.
`state.provider !== 'groq'` 한 줄로 거른다.

### ③ 지연값 절반 — 이제 순수한 연출이다

```js
replyDelayMs: [600, 1600] → [300, 800]
chainDelayMs: [900, 2200] → [450, 1100]
```

원래 이 지연은 API 응답 대기를 사람처럼 보이게 **덮는** 용도였다.
이제 기다릴 게 없으니 연출만 남았고, 짧아도 어색하지 않다.

### ④ 2단 모델 — 제안이자 구현

요청서에서 "검토 · 계획에 제안 포함"이라고 한 항목이다.
제안만 하기엔 효과가 확실해서 **켤 수 있는 형태로 구현하고 기본을 켬으로 뒀다.**

근거: 벨챗 메시지의 상당수는 `filler`(리액션)·`casual`(잡담)이고,
이 무게의 지시문은 "**딱 한 줄**", "1~2문장이 기본" 이다.
`ㅋㅋ 그러게` 한 줄을 위해 큰 모델을 깨우는 건 낭비다 — 비용보다 **지연**이 아깝다.

```js
const SPEED_RULES = {
  tierByWeight: { filler: 'fast', casual: 'fast', normal: 'main', serious: 'main' }
};
```

- Anthropic → Haiku 4.5
- Gemini → Flash-Lite
- Groq → GPT-OSS 20B

`fastModel` 이 없는 provider 는 자동으로 항상 메인 모델을 쓰고,
설정의 체크박스도 비활성으로 흐려진다. 동작 안 하는 컨트롤을 보여주지 않는다.

**안건(`serious`)은 절대 소형 모델로 내려가지 않는다.** 속도를 위해 품질을 깎는
지점을 무게 기준으로 명확히 그은 것이다. 설정 → 일반에서 끌 수 있다.

### ⑤ 언어 가드 발생률 집계

재생성 한 번은 **API 호출 한 번**이다. 잦으면 그 자체가 느림의 원인이다.
그래서 눈에 보이게 셌다.

```
🈯 한국어 재생성 3/12회 (25%) · 회복 2 · 실패 1 · 평균 +820ms
   — 재생성이 잦습니다. 모델을 바꾸면 더 빨라집니다.
```

설정 → 일반 하단에 뜬다. 20% 를 넘으면 경고 문구가 붙는다.
콘솔에서는 `window.__velchatLangStats()` 로 방아쇠별 내역까지 볼 수 있다.

이 숫자가 높은 모델은 "느린 모델"이다 — 응답 자체가 느린 게 아니라
매번 두 번 부르고 있는 것이다. 모델 선택의 근거로 쓸 수 있다.

---

## 4. 출처 발언 규칙

### 실제로 났던 사고

페르소나가 "네이버 평점 4.5점" 같은 **확인하지 않은 수치**를 지어냈다.
그럴듯해서 더 위험하다 — 사용자가 그걸 믿고 판단한다.

### 원인은 프롬프트끼리 싸우고 있었다는 것

기존 `COMMON_RULES` 와 `MESSAGE_WEIGHTS` 가 이렇게 시키고 있었다.

> "실제 사례·**수치**·방법을 하나는 든다"
> "구체적으로. 실제 이름·제품·기관·방법·**수치**를 든다"

모델 입장에서 수치를 모르면 선택지가 둘이다 — 지시를 어기거나, 지어내거나.
**규칙을 하나 더 붙이기 전에, 지어내라고 등을 떠밀던 문장부터 고쳐야 했다.**

```
"실제 사례·수치·방법을 하나는 든다"
  → "실제 사례·방법을 하나는 든다. 수치는 확실히 아는 것만 —
     모르면 숫자를 빼고 네 경험·판단으로 말해라."

"구체적으로. 실제 이름·제품·기관·방법·수치를 든다"
  → "아는 범위에서 실제 이름·제품·기관·방법을 든다.
     구체적으로 말하라는 건 지어내라는 뜻이 아니다."
```

### 그 위에 전용 블록

`[출처·수치 — 지어내면 그 답변은 무효다 · 매우 중요]`

- 금지 목록을 **실제 사고 유형으로** 적었다 (추상적인 "정확하게 말하라"는 안 먹힌다)
  - 확인하지 않은 평점 — "네이버 평점 4.5점", "구글 리뷰 4.2점"
  - 출처 없는 통계·비율 — "재방문율 70%", "매출 30% 상승"
  - 존재를 확신할 수 없는 자료 인용 — "○○ 보고서에 따르면"
  - 본 적 없는 기사·논문·기관 이름, 그럴듯하게 지어낸 URL
- **대체 화법을 짝으로 준다.** 금지만 하면 모델은 입을 닫는다.
  - "재방문율이 70%예요" → "단골이 꽤 붙는 편이에요"
  - "네이버 평점 4.5점이던데" → "리뷰 반응은 괜찮아 보이던데"
- 모르면 의견 톤으로 — "제가 해보니까", "현장에서 보면", "확실친 않은데 감으로는"

### 사족 금지가 이 규칙의 핵심이다

> "출처가 정확하진 않지만", "정확한 수치는 아니지만", "어디서 봤는데"
> 같은 말을 붙여서 지어낸 수치를 말하지 마라.
> 그런 단서를 달아야 할 정도면 **그냥 말하지 않는 게 맞다.**
> 단서 + 지어낸 수치 = 그래도 거짓말이다.

이걸 막지 않으면 모델은 규칙을 "면책 문구를 붙이면 된다"로 해석한다.
사용자에게 남는 건 **여전히 그 숫자**다.

### 겁먹게 만들지는 않는다

> 이 규칙은 "겁먹고 얼버무려라"가 아니라 **"모르는 걸 아는 척하지 마라"**는 뜻이다.
> {chair}가 준 정보(사진·앞선 대화에 나온 수치)는 마음껏 인용해도 된다.

과잉 교정도 실패다. 온통 "제 생각엔"만 붙은 답변은 쓸모가 없다.

---

## 5. 확인한 것

### 자동 테스트 — 44건 통과

HTML 에서 함수 소스를 떼어내 Node 에서 실제로 돌렸다 (문법 검사가 아니라 동작 검사다).

**`test-v28.js` — 23건**
Gemini 형식 변환 6 · SSE 파서 3 (청크 경계 분할, `\r\n` 구분자, 깨진 JSON) ·
provider 3종 델타 추출 5 · 논스트리밍 추출 3 · 오류 분류 6

> 여기서 **실제 버그를 하나 잡았다** — 빈 문자열 content 를 Gemini 가 거절하는 문제.
> Anthropic·Groq 은 조용히 넘어가서 Gemini 에서만 터졌을 것이다.

**`test-v28b.js` — 21건**
2단 모델 선택 8 (끔·`fastModel` 없음·이미 빠른 모델 등 경계 포함) ·
인터럽트 정책 6 (말한 턴 보존 / 침묵 턴 중단 / 연속 전송) ·
스트림 따라잡기 수렴 4 · CONFIG 값 3

**`regress.js` — 36건 (회귀)**
v2.7.2 키보드 6 · v2.7.1 창 모드 3 · v2.5 몰입/크기 3 · v2.3 언어 가드 6 ·
v2.2 사진 3 · v2.4 테마 2 · v2.7.0 키 UX 5 · v2.8 전 provider 커버리지 8

`<style>` 중괄호 균형, `<script>` 두 블록 파싱도 통과.

### 실기기·실호출은 못 했다

이 세션에서 Chrome 확장이 연결되지 않았고, **API 키도 없어 실제 호출은 하지 못했다.**
SSE 파싱과 형식 변환은 합성 데이터로 검증했지만, 진짜 응답으로는 확인이 필요하다.

**남은 확인 목록**

1. Gemini 키 등록 → 연결 테스트 → 실제 대화 (스트리밍으로 글자가 흐르는지)
2. Gemini 에 사진 첨부 → 페르소나가 사진 내용을 실제로 언급하는지
3. 응답 3명 중 1명이 말하는 도중 새 메시지 전송
   → 말하던 사람은 끝까지, 대기 중이던 2명은 사라지고, 새 메시지로 라우팅
4. 연속 3연타 전송 → 마지막 메시지 기준으로만 답이 나오는지
5. 설정 → 일반에서 2단 모델 체크박스 켬/끔, provider 별 안내 문구
6. 잡담 몇 번 주고받은 뒤 재생성 발생률(🈯) 숫자가 오르는지
7. 페르소나에게 평점·통계를 유도하는 질문 → 숫자를 지어내는지, 의견 톤으로 피하는지
8. **회귀** — 폰에서 키보드 올림(v2.7.2) · 설치 창 꽉 참(v2.7.1) · 몰입 모드 접기(v2.5) ·
   사진 압축(v2.2) · 테마 전환(v2.4)

### 알려진 한계 / 되돌리는 법

- **Anthropic 스트리밍 + 웹 검색** — 검색이 끝나야 첫 텍스트가 나오므로
  스트리밍 이득이 다른 provider 보다 작다. 구조적인 것이라 손댈 수 없다.
- **Gemini 모델 ID** — Google 이 모델을 자주 갈아치운다.
  404 가 나면 `LLM_CONFIG.gemini.models` 의 id 만 고치면 된다.
- **동시 호출과 요금** — 미리 걸기는 취소해도 이미 나간 요청은 과금될 수 있다.
  `AbortController` 로 끊지만 서버가 이미 처리를 시작했으면 그만큼은 청구된다.
- 문제가 생기면 `SPEED_RULES` 의 `stream` / `prefetch` / `twoTier` 를 `false` 로 바꾸면
  **v2.7 동작(순차 · 완성 후 표시 · 단일 모델)으로 그대로 돌아간다.**
  `INTERRUPT_RULES` 도 마찬가지로 정책만 바꾸면 된다.

---

## 손댄 곳

| 파일 | 내용 |
|---|---|
| `vela-boardroom-prototype.html` | Gemini provider · 스트리밍 · 미리 걸기 · 인터럽트 · 2단 모델 · 출처 규칙 · 재생성 집계 · 버전 2.8.0 |
| `vela-boardroom-sw.js` | 스탬프가 `BUILD_ID` 갱신 |
| `velchat-v2.8.0-notes.md` | 이 문서 |
