# 벨챗 v2.7.0 — API 키 등록 편의 개선

> 2026-08-14 · 대상 파일: `vela-boardroom-prototype.html` (단일 파일 앱)
> 이전 버전: v2.6.0 (모바일 헤더 정렬 + 웰컴 멘트 + 페르소나별 목소리)

키를 넣는 순간이 이 앱에서 가장 잘 막히는 지점이다. 그 네 가지를 전부 손봤다.

---

## 1. 📋 붙여넣기 버튼

키 입력란 오른쪽에 `📋` 버튼 하나. 탭하면 `navigator.clipboard.readText()` 로
클립보드를 읽어 바로 채운다.

- 붙여넣은 값이 **QR 페이로드(`VC1|provider|key`)면 provider 까지 같이 맞춘다.**
- 클립보드 권한이 없거나 실패하면 안내를 띄운다
  ("입력란을 길게 눌러 직접 붙여넣어 주세요") — 막다른 길을 만들지 않는다.
- 옆에 `👁` 버튼도 뒀다. 키가 `password` 로 가려져 있어 오타 확인이 안 되던 문제.

---

## 2. 공백·줄바꿈 자동 제거

**복붙 실패의 최다 원인이다.** 메일·메신저에서 키를 복사하면 앞뒤 공백은 물론
줄 가운데 개행이 끼어 들어온다. API 키에는 공백이 들어갈 수 없으므로 전부 걷어낸다.

```js
function cleanApiKey(raw) {
  let s = String(raw ?? '');
  s = s.replace(/[\s ​-‍﻿]+/g, '');   // 모든 공백 + 제로폭 문자
  s = s.replace(/^(Bearer|bearer|Authorization:|authorization:)/, '');
  s = s.replace(/^["'`“‘]+|["'`”’]+$/g, '');              // 따옴표
  s = s.replace(/[,;]+$/, '');                            // 꼬리 문장부호
  return s;
}
```

` `(줄바꿈 없는 공백)과 `​~‍`(제로폭)까지 잡는 게 핵심이다.
**눈에 보이지 않아서 사용자가 원인을 절대 못 찾는 종류의 실패**다.

적용 지점:
- 입력란 `input` · `paste` 이벤트에서 실시간으로 (커서 위치 보존)
- 저장할 때 한 번 더
- QR 스캔·클립보드 붙여넣기 경로

> 실측 8종 전부 통과 — 앞뒤 공백 / 중간 개행 / `\r\n` + 공백 / 따옴표 감싼 키 /
> `Bearer ` 접두 / 꼬리 쉼표 / 제로폭+NBSP 혼합 / 공백만 있는 경우

---

## 3. 연결 테스트 (저장 시 자동 실행)

키가 오타인지 아닌지 **그 자리에서** 알 수 있게 했다.

- `max_tokens: 1` 로 최소 요청을 한 번 보낸다 (비용 사실상 0)
- 저장 버튼을 누르면 자동 실행. **성공하면 모달이 닫히고, 실패하면 열어둔다** —
  바로 고칠 수 있게. `🔌 연결 테스트` 버튼으로 따로 돌릴 수도 있다.

### 응답 구분이 핵심
"실패"를 뭉뚱그리면 사용자가 뭘 고쳐야 할지 모른다. 원인별로 나눴다.

- **401** → "키가 올바르지 않습니다 — 오타이거나 만료된 키입니다"
- **403** → "이 키에 권한이 없습니다"
- **429** → **"키는 유효합니다.** 다만 지금 사용 한도를 넘었습니다" (성공으로 취급)
- **잔액 부족** → **"키는 유효합니다.** 다만 잔액이 부족합니다" (성공으로 취급)
- **404 / model 오류** → "모델 이름을 확인해주세요"
- **네트워크 예외** → "네트워크에 닿지 못했습니다 — 인터넷 연결을 확인해주세요"

429·잔액 부족은 **키 자체는 멀쩡한 상황**이라 실패로 표시하면 사용자가 멀쩡한 키를
계속 다시 입력하게 된다. 그래서 `ok: true, warn: true` 로 따로 뒀다.

> 실측 5개 시나리오 전부 기대대로 분기

---

## 4. PC → 폰 QR 전송

### 왜 QR인가
키를 폰에 옮기는 흔한 방법(카톡으로 보내기, 메일로 보내기)은 전부 **키가 서버를 거친다.**
QR은 화면에 그렸다가 카메라로 읽는 것뿐이라 **주소창·네트워크·서버 어디에도 남지 않는다.**

### QR 인코더를 직접 구현했다
단일 파일 앱이라 외부 라이브러리를 쓸 수 없다 (오프라인 동작·CSP). 그래서
**byte 모드 / 버전 1~10 / 오류정정 레벨 M** QR 인코더를 규격대로 작성했다.

- GF(256) 산술 + 리드-솔로몬 부호화
- 블록 분할·인터리빙 (버전별 블록 구성 테이블)
- 파인더·타이밍·정렬 패턴, 다크 모듈, 포맷 정보 BCH(15,5), 버전 정보 BCH(18,6)
- 마스크 8종 전부 적용 후 페널티(4규칙) 최소인 것 선택

버전 7-M 이 122바이트까지 담아 Anthropic 키(약 108자)에 여유가 있다.

### 작성 중 잡은 버그 2건
검증기를 돌리기 전 코드 검토에서 발견한 것들이다. 둘 다 **조용히 깨진 QR을 만드는** 종류다.

1. **버전 정보 BCH 루프가 12번 돌았다** (6번이어야 함).
   데이터 6비트만큼만 나눠야 하는데 12번 돌면 `0x1F25 << (5-i)` 의 시프트가 음수가 되고,
   JS 는 이를 `<< 31` 로 해석해 나머지를 통째로 망가뜨린다. → **버전 7 이상이 전부 불량**
2. **포맷 정보 사본 ②의 비트 7이 잘못된 자리에 갔다.**
   `i < 8` 조건 때문에 비트 7이 다크 모듈 자리 `(size-8, 8)` 를 덮고,
   정작 들어가야 할 `(8, size-8)` 은 비어 있었다. → `i < 7` 로 수정

### 검증 — 독립 검증기로 되읽기
이 환경(Windows 데스크톱 Chrome)에는 `BarcodeDetector` 가 없어 카메라 왕복 테스트가
불가능했다. 대신 **만들어진 심볼을 규격대로 되읽는 검증기**를 따로 작성해 확인했다.

- 파인더 3개 · 타이밍 패턴 · 다크 모듈 위치 ✓
- **포맷 정보 두 사본을 각각 되읽어 BCH 검사** — 값 일치, 유효, EC=M, 마스크 번호 일치 ✓
- 코드워드 개수가 버전별 규격값과 일치 (26 / 134 / 196) ✓
- **디인터리브 후 각 블록의 RS 신드롬이 전부 0** ✓ (부호가 실제로 유효하다는 증거)
- 마스크 해제 → 지그재그 역주행 → 비트 복원 → **원문 완전 일치** ✓
- 마스크 선택이 데이터에 따라 실제로 달라짐 (2, 2, 2, 1) ✓

4종 페이로드(11자 / 65자 / 107자 / 122자, 버전 1·5·7) 전부 통과.

### 표시 쪽 (PC)
설정 → `📱 키를 QR로 표시`

- 키가 그대로 담기므로 **경고 문구**를 위에 붙였다
- **60초 뒤 자동으로 닫힌다** (어깨너머 노출 방지). 남은 시간을 표시한다
- 닫으면 캔버스를 비운다 — 화면에 키가 남지 않게
- 페이로드 형식: `VC1|<provider>|<key>` — provider 까지 같이 넘어간다

### 스캔 쪽 (폰)
설정 → `📷 QR 스캔`

- `BarcodeDetector` + `getUserMedia({facingMode:'environment'})` 로 후면 카메라
- 읽으면 provider 자동 선택 + 키 입력 + **바로 연결 테스트까지 실행**
- 벨챗 형식이 아니면 계속 스캔하며 안내만 바꾼다
- 모달을 닫으면 카메라 트랙을 반드시 정지시킨다

**iOS 사파리는 `BarcodeDetector` 를 지원하지 않는다.** 그 경우 대안을 안내한다:
> 기본 카메라 앱으로 QR을 비추면 텍스트가 뜹니다. 복사한 뒤 📋 붙여넣기를 눌러주세요.

URL이 아니라 **평문**을 담았기 때문에 iOS 카메라 앱이 주소창을 열지 않고 텍스트로 보여준다.
(요구사항의 "주소창을 거치지 않는다"와도 맞다)

---

## 검증 요약

| 항목 | 결과 |
|---|---|
| 키 정리 8종 | 전부 통과 |
| 입력란 실시간 스크럽 | `  sk-ant-api03-\nHELLO  ` → `sk-ant-api03-HELLO` |
| QR 페이로드 왕복 | provider·키 완전 복원, 맨 키(sk-ant-/gsk_)도 인식, 쓰레기는 null |
| QR 인코더 | 4종 페이로드 규격 검증 통과 (RS 신드롬 0, 포맷 BCH 유효, 원문 일치) |
| QR 표시 | 캔버스 318×318, 경고·타이머·메타 표시, 닫으면 캔버스 제거 |
| 연결 테스트 5종 | 성공 / 401 / 429(유효) / 잔액부족(유효) / 네트워크 각각 정확히 분기 |
| 스캔 미지원 안내 | BarcodeDetector 없는 환경에서 대안 안내 노출 |
| 회귀 | 몰입 모드·크기 조절·헤더 한 줄·스크롤 정책·언어 가드·테마·사진 이상 없음 |
| 콘솔 에러 | 0건 |

**실제 폰 카메라로 스캔하는 왕복은 이 환경에서 확인하지 못했다** — 데스크톱 Chrome 에
`BarcodeDetector` 가 없기 때문이다. 심볼 자체는 규격 검증을 통과했으므로 표준 QR 리더가
읽을 수 있는 상태다.

---

## 변경 파일

- `vela-boardroom-prototype.html` — 전부 여기
- `vela-boardroom-sw.js` — 스탬프가 BUILD_ID 갱신
- `velchat-v2.7.0-notes.md` — 이 문서

## 유지보수 메모

- **키 정리 규칙 추가** = `cleanApiKey()` 에 `replace` 한 줄
- **연결 테스트 응답 분기 추가** = `testApiKey()` 의 상태 코드 분기
- **QR 용량이 모자라면** `QR_TOTAL_CW` / `QR_M_DATA_CW` / `QR_M_EC_PER_BL` /
  `QR_M_BLOCKS` / `QR_ALIGN` / `QR_REMAINDER` 에 버전 11 이상 행을 추가하면 된다
  (여섯 테이블 모두 같은 순서로 늘려야 한다)
- QR 페이로드 형식을 바꿀 땐 `KEY_QR_PREFIX` 와 `parseKeyPayload()` 를 함께 고칠 것.
  구버전 폰이 신버전 PC의 QR을 못 읽게 되므로 접두사 버전을 올리는 게 안전하다
