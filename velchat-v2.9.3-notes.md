# 벨챗 v2.9.3 — 안정화 + 측정 + 데이터 계층

2026-08-17 · `vela-boardroom-prototype.html`

한 줄 요약: **무한 대기를 끊고, 재는 눈을 붙이고, 환율·날씨는 LLM을 거치지 않고 직접 받아온다.**

외부 리뷰 결론대로 순서를 지켰다 — 무한대기 → 호출량 → 구조화 데이터 → 검증 → 측정.

---

## 1. [A] 무한 대기의 진짜 원인

v2.9.2의 대기 예산 로직은 멀쩡했다. **문제는 그 로직에 도달하지 못한 것**이었다.

`resolveRoute` 의 auto 경로 마지막 줄:

```js
const ready = usable.filter(providerAvailable);
if (ready.length) return build(ready[0]);
if (!usable.length) return null;
const soonest = usable.slice().sort(...)[0];
return build(soonest);          // ← 쿨다운 중인데도 돌려준다
```

Gemini가 429로 쿨다운 중이어도 **route가 나온다.**
→ `!route && strict` 대기 경로로 안 가고 곧바로 재호출 → 또 429 → 12초 대기 → 재호출 → …
`guard < 8` 이라 8회 호출 + 7회 대기 ≈ 84초. 사용자 눈에는 무한 반복이다.

**게다가 그 8번의 호출이 429를 한 건씩 더 생산했다.** 한도를 태우면서 기다린 셈이다.

### 고친 방법 (세 겹)

**① 종료 판단을 한 함수로 모았다 — `planFreshnessWait()`**
v2.9.2는 이 판단이 두 군데에 흩어져 있었고 계산식도 미묘하게 달랐다.
그래서 "언제 그만두는가"가 코드에서 한눈에 안 보였고, 실제로 종료 조건 하나가 빠졌다.
이제 한 함수가 네 가지를 전부 본다:

```
① 시간 예산(90초) 소진
② 대기 횟수(5회) 초과      ← 3초씩 30번 기다리는 경우를 막는다
③ 사용자가 "그만" 을 눌렀다
④ 검색 되는 곳이 전부 서킷 차단 — 기다려도 열릴 곳이 없다
```

**② 서킷 브레이커**

쿨다운은 순서를 미룰 뿐 후보 목록에는 남는다. 그래서 갈 곳이 없으면 결국 다시 두드린다.
서킷은 **두드리는 것 자체를 막는다.**

```
CLOSED → 연속 3회 429/503 → OPEN(60초, 호출 금지) → 시간 경과 후 시험 호출 1회
       → 성공하면 CLOSED / 실패하면 즉시 다시 OPEN
```

설계 주의점 하나: `circuitAllows()` 는 **부작용 없는 순수 판정**이다.
`resolveRoute` 의 filter에서 여러 번 불리기 때문에, 여기서 상태를 바꾸면
"보기만 했는데 시험 기회가 소모되는" 일이 생긴다. 처음엔 `probing` 플래그를 뒀다가 그 이유로 걷어냈다.

**③ strict 모드에서는 `soonest` 폴백을 쓰지 않는다**
1절의 직접 수정. 쿨다운·차단 중이면 `null` 을 돌려 정상적으로 대기 경로로 보낸다.

### "그만" 버튼

대기 중 말풍선 머리에 붙는다. **취소와 다르다 — 답은 받고 검색만 포기한다.**
기다리다 지친 사람에게 "취소"밖에 없으면 대화가 끊긴다.

```
🔍 사용량이 많아 47초 뒤 찾아볼게요   [그만]
```

새 메시지가 들어오면 기존 `cancelStaleTurns` 가 대기를 끊는다 (추가 작업 없이 확인만 했다).

---

## 2. [B] 측정 — 고치기 전에 잰다

설정에 **📊 측정** 패널을 붙였다. 오늘 상세 + 지난 7일 한 줄 요약.

```
오늘 (2026-08-17)
메시지 24 · LLM 호출 31
메시지당 1.29콜 (목표 1.0 이하)      ← 1.0 이하 초록 / 1.5 초과 빨강
검색 6 · 데이터API 4 · 수치정정 0
429 2 · 503 1 · 재시도 3 · 서킷 1회
응답 평균 2.1초 · P95 6.4초
```

**핵심 지표는 `CALLS_PER_MESSAGE`다.** 무료 한도는 토큰이 아니라 **호출 수**로 소모되기 때문이다.
목표(1.0)를 화면에 같이 박아뒀다 — 숫자만 보여주면 그게 좋은 건지 나쁜 건지 알 수 없다.

**P95를 버킷으로 근사한 이유**: 응답시간 원본을 다 저장하면 localStorage가 커진다.
버킷 9칸 카운터면 하루치가 100바이트 남짓이라 14일을 담아도 2KB 이하다.
정확한 분위수는 못 구하지만 "어느 구간에 95%가 들어오는가"는 알 수 있고, 판단에는 그걸로 충분하다.

측정 저장은 1.5초 디바운스로 묶고, 실패해도 조용히 넘어간다 — **측정이 앱을 막으면 안 된다.**

---

## 3. [C] 데이터 계층 — API → Fact → LLM → Validator

### 흐름

```
"환율 얼마야?" → 신선도 판정 → DATA_SOURCES 매칭 → 공식 API 직결
              → Fact 정규화 → 프롬프트에 "이 값을 바꾸지 말고 설명하라"
              → LLM 1콜 (검색 없음) → Validator 로 수치 대조
```

**이득이 셋이다.** 검색 호출 0 (`callsPerMessage` 1.0), 지연 없음,
그리고 **숫자가 출처와 일치함을 앱이 보증한다.**

### 실측 확인

```
환율: {"value":1411.27,"display":"1,411.27원","label":"미국 달러 1달러",
       "asOf":"2026-08-14","asOfText":"8/14 기준","sourceName":"국제 기준환율(ECB)"}
날씨: {"value":25.7,"display":"25.7도 · 이슬비","label":"부산 현재 기온",
       "asOf":"2026-08-17T20:15","asOfText":"8/17 20시 관측"}
```

두 API 모두 키가 필요 없고 `access-control-allow-origin: *` 이라 브라우저에서 바로 호출된다.

### asOf 라벨 — 말풍선 하단에 항상 펼쳐 둔다

**오늘(8/17) 조회인데 환율 날짜가 8/14다.** ECB 계열은 영업일 하루 한 번 갱신이라
월요일에 물으면 금요일 값이 나온다. 이걸 안 보여주면 **정확한 소스를 쓰고도 v2.9.1과 똑같은 사고**가 난다 —
시점 없는 숫자는 전부 "지금 값"으로 읽히기 때문이다.

그래서 이 줄만은 접지 않는다:

```
8/14 기준 · 국제 기준환율(ECB) · 실거래·은행 환전가와 다를 수 있어요
```

기준일은 굵게 강조하고, 출처명은 링크로, 주의문구는 그 뒤에 잇는다.

### 도시 좌표는 내장했다

open-meteo 지오코딩이 한글 검색에서 엉뚱한 곳을 준다 (실측: **"부산" → 36.38/128.37, 경북 어딘가**).
그래서 지오코딩 API를 쓰지 않고 주요 도시 24곳 좌표를 CONFIG에 넣었다. 왕복도 하나 줄었다.

### Validator

```
정답(1,411.27원)              → 통과
틀린 숫자(1352원)             → 불일치 → 재생성 1회 → 실패 시 "1,411.27원" 으로 교정
"작년엔 1,100원이었죠"         → 통과 (건드리지 않는다)
숫자를 안 씀                  → 통과
```

**오탐이 제일 위험하다.** 과거 언급까지 고치면 맞는 문장을 망가뜨린다.
그래서 Fact 값 **±20% 안의 숫자만** "지금 값을 말하려는 시도"로 보고 검사한다. 그 밖은 손대지 않는다.
교정했을 때는 반드시 화면에 남긴다 — `※ 숫자는 출처 기준으로 정정했습니다`.
조용히 고치면 사용자가 무엇을 믿어야 할지 알 수 없다.

교정 횟수는 `corrections` 지표로 세므로, 과하면 규칙을 되돌릴 근거가 된다.

### 폴백 3단

```
데이터 API 성공 → Fact 주입 (검색 0콜)
데이터 API 실패 → LLM 웹 검색 (v2.9.2 경로 그대로)
검색도 실패     → 비상 화법
```

데이터 API는 4초 타임아웃. **"빠르려고 붙인 계층"이 느림의 원인이 되면 안 된다.**
`fetchFact` 는 예외를 던지지 않는다 — 데이터 소스 장애가 대화를 막으면 안 되기 때문이다.

---

## 4. [D] 못 지킬 약속 금지

이 방의 페르소나는 **나중에 혼자 확인할 수단이 없다.** 대화가 끝나면 멈춘다.
그러니 "이따 알려주겠다"는 전부 거짓말이 된다.

| 금지 | 대체 |
|---|---|
| "확인해볼게요" | "잠시 뒤에 다시 물어봐 주시면 찾아볼게요" |
| "알아보고 알려드릴게요" | "지금은 확인이 잘 안 되네요. 다시 한번 물어봐 주세요" |
| "잠시만요, 찾아볼게요" | "지금 숫자는 확인이 필요해요" |
| "이따 알려드릴게요" | "조금 뒤에 다시 여쭤보시면 그때 찾아볼게요" |
| "곧 업데이트해서" | (쓰지 않는다) |

핵심 한 줄로 규칙에 넣었다:
> **다음 행동은 항상 {chair}가 다시 물어보는 것이다.**
> 네가 무언가를 하겠다고 하지 말고, {chair}가 무엇을 하면 되는지를 말해라.

---

## 5. 공유 분석 구조 — 자리만 만들어 뒀다

`FEATURES.sharedTurn = false` (미구현).

**제안된 "분석 1콜 → 2명 발언 2콜"은 총 3콜이라 KPI가 오히려 나빠진다** (1.15 → 1.5).
토큰은 줄어도 호출 수가 늘고, 무료 한도의 단위는 호출 수다.

대안은 **한 콜에서 두 사람 발언을 JSON으로 함께 받아 말풍선 2개로 나누는 것**(1콜).
다만 한 번의 생성으로 두 사람을 쓰면 **말투가 수렴한다** — 5인 체계의 핵심 가치를 정면으로 깎는다.
완화책(양쪽 few-shot 동시 주입 / 말투 검사 후 재생성 / A-B 플래그)은 계획서 5절에 정리했다.

측정 기반이 막 생겼으니, **다음 버전에서 A/B로 검증한 뒤 켜는 게 순서다.**

---

## 6. 검증

- 소스 선택·통화·도시 인식 전부 정확 (`엔화→JPY`, `부산 날씨→부산`, `코스피→검색 경로`)
- 실제 API 왕복 성공, 캐시 두 번째 조회 0ms
- Validator 5개 케이스 통과 (과거 언급 보존 포함)
- 서킷: 2회까지 허용 → 3회째 OPEN → 라벨 "차단 중 (60초 남음)" → 성공 시 복구
- 대기 계획 4가지 종료 조건 전부 동작
- v2.9 회귀 없음 — 5인 명단·아카이브(정민철)·TTS 7종·잡담 로테이션·담당자 고정
- 인라인 스크립트 문법 오류 0

**실기기 확인 미완** (브라우저 확장 미연결): 측정 패널·출처 줄·"그만" 버튼의 실제 렌더,
그리고 실제 429 상황에서의 서킷 동작. 첫 실행 때 설정 → 📊 측정을 열어 확인하는 걸 권한다.

### 관찰 하나

`"오늘 날씨 좋다"` 같은 **감탄문에도 신선도 트리거가 걸린다.** 설계상 의도된 동작이고
(데이터 API는 무료·무키라 비용이 거의 없다) 실제 기온을 알고 답하는 게 어색하지도 않다.
다만 어색한 답이 나오면 `FRESHNESS_TRIGGERS.날씨` 를 질문형으로 좁히면 된다.

---

## 7. 조정하는 법

| 원하는 것 | 방법 |
|---|---|
| 데이터 소스 추가 | `DATA_SOURCES` 에 한 항목 (`match`·`url`·`parse`) |
| 도시 추가 | `KO_CITIES` 에 한 줄 |
| 서킷 민감도 | `CIRCUIT_RULES.failThreshold` / `openMs` |
| 서킷 끄기 | `CIRCUIT_RULES.enabled = false` |
| 대기 상한 | `FRESHNESS_RULES.maxWaits` / `waitCapMs` |
| 검증 끄기 | `VALIDATOR_RULES.enabled = false` |
| 교정만 끄기 (검사는 유지) | `VALIDATOR_RULES.fixInPlace = false` |
| 오탐이 많다 | `VALIDATOR_RULES.window` 를 좁힌다 (기본 0.20) |
| 측정 끄기 | `METRICS.enabled = false` |
