# 벨챗 v2.9.4 — 환율 대기 재발 조사 + Fact 우선 · 숫자 절대 보존

2026-08-17 · `vela-boardroom-prototype.html`

한 줄 요약: **두 사전이 따로 놀아 데이터 API가 안 불렸다. 그리고 값을 손에 쥐고도 못 보여주던 구멍을 막았다.**

---

## 1. 재현 조사 — 데이터 API 경로가 안 탄 조건

문장 21개 변형으로 실측했다. 결과는 명확했다.

### 구조적 결함: 사전이 두 개인데 서로 모른다

v2.9.3의 데이터 API 호출 조건은 이랬다:

```js
if (searchInfo.freshness && !hasImages) { fact = await fetchFact(question); }
```

**`freshnessHit()` 이 걸려야만 `fetchFact` 가 불린다.** 그런데 두 사전이 따로 관리됐다:

```
FRESHNESS_TRIGGERS.환율 : 환율, 달러값, 원달러, 엔화, 엔저, 유로화, 달러 얼마, 달러가 얼마
DATA_SOURCES.fx.match   : 환율, 달러, 달러값, 원달러, 엔화, 엔저, 유로화, 위안화, 파운드
                                 ^^^^                              ^^^^^^  ^^^^^^
                                 소스에만 있는 말 = 데이터 API 를 못 탄다
```

→ **소스가 멀쩡히 답할 수 있는 질문인데 데이터 API 가 아예 호출되지 않았다.**

### 실측된 구멍 (v2.9.3 기준)

| 문장 | 신선도 | 데이터 소스 | 결과 |
|---|---|---|---|
| `1달러 몇 원이야?` | ✗ | fx 있음 | **미호출 → 검색 경로** |
| `달러 지금 몇 원` | ✗ | fx 있음 | **미호출 → 검색 경로** |
| `밖에 추워?` | ✗ | weather 있음 | **미호출 → 검색 경로** |
| `미국 돈 얼마야` | ✗ | ✗ | 검색 경로 |

`"환율 얼마야?"`, `"환율 알려줘"` 등은 v2.9.3에서도 데이터 API를 정상적으로 탔다.
즉 **질문 표현이 조금만 달라져도** 검색 대기 경로로 빠지는 구조였다.

### 두 번째 구멍: 값을 쥐고도 못 보여준다

더 심각한 건 이쪽이다. 데이터 API가 **성공해서 1,411원을 손에 쥔 상태**에서
LLM이 한도에 막히면, `presentTurn` 이 quiet 실패로 말풍선을 지우고 시스템 칩만 남겼다.

**앱이 정답을 알면서 사용자에게 아무것도 안 보여준 것이다.**
이건 "모르는 것"이 아니라 "알면서 숨기는 것"에 가깝다.

---

## 2. 수정 — 사전을 하나로

`freshnessHit()` 이 `DATA_SOURCES.match` 도 함께 본다.

```js
// 데이터 소스가 답할 수 있는 질문이면 그것도 "지금 값"을 묻는 것이다
const src = pickDataSource(text);
if (src) return { category: src.def.label, word: src.matched, fromSource: src.kind };
```

이제 **소스에 단어를 추가하면 신선도로도 자동으로 잡힌다.** 사전을 두 번 고칠 일이 없다.

그리고 Fact 조회 조건 자체를 `freshness` 가 아니라 **데이터 소스 매칭**으로 바꿨다 —
소스가 답할 수 있으면 무조건 먼저 값을 확보한다 (Fact 선확보 원칙).

빠진 표현도 채웠다: `미국 돈`·`일본 돈`·`중국 돈`, `쌀쌀`·`포근`·`기상`·`비 올`·`눈 올`.

> `몇 원` 같은 일반 단위는 **일부러 넣지 않았다.** "이거 몇 원이야?"까지 환율로 잡힌다.
> `"1달러 몇 원이야?"` 는 `달러` 로 이미 걸리므로 넣을 필요도 없다.

### 결과: 21/21 통과

```
1달러 몇 원이야?   → ✅ 데이터API   (v2.9.3: 미호출)
달러 지금 몇 원    → ✅ 데이터API   (v2.9.3: 미호출)
밖에 추워?        → ✅ 데이터API   (v2.9.3: 미호출)
미국 돈 얼마야     → ✅ 데이터API   (v2.9.3: 검색)
```

---

## 3. 숫자 절대 보존

Fact를 확보한 뒤 LLM이 전부 막히면, 비상 화법으로 **숫자를 버리지 않는다.**
설명은 포기하되 값은 그대로 내보낸다.

```
강지원
지금 자세한 설명은 어렵지만, 조회된 환율은 1달러 = 1,411.27원이에요.
─────────────────────────────────────────────
8/14 기준 · 국제 기준환율(ECB) · 실거래·은행 환전가와 다를 수 있어요
```

출처 줄이 평소와 똑같이 붙으므로 **그 값을 어떻게 받아들여야 하는지도 함께 전달된다.**

날씨도 같다:

```
지금 자세한 설명은 어렵지만, 부산은 26도, 비예요.
8/17 22시 관측 · Open-Meteo 기상 데이터 · 관측 시각 기준이라 지금과 조금 다를 수 있어요
```

### 곁가지 처리

- 이 턴(`factOnly`)에는 **연쇄 반응을 붙이지 않는다.** LLM이 막혀서 나온 결과인데
  한 콜을 더 쓰면 그것도 막힌다.
- 남은 응답자도 접는다. 같은 벽에 부딪힐 게 뻔하다.

### 검증하다 찾은 한국어 오류 두 개

실제 출력이 이랬다:

```
"부산는 26도, 맑음예요."     ← 조사가 둘 다 틀렸다
```

`withParticle()` 을 만들어 받침으로 조사를 고른다 (한글 음절은 `(코드-0xAC00) % 28` 로 판별).

```
부산은 / 서울은 / 대전은 / 제주는     ✓
맑음이에요 / 비예요 / 대체로 맑음이에요  ✓
```

사용자에게 그대로 보이는 문장이라 반드시 고쳐야 했다.

---

## 4. 폴백 순서 (변경 없음, 확인만)

```
데이터 API 성공 → Fact 주입 → LLM 설명 (검색 0콜)
                          └ LLM 전부 실패 → ★ Fact 직접 표시 (v2.9.4 신규)
데이터 API 실패 → LLM 웹 검색 → 실패 시 비상 화법
```

데이터 API 자체가 실패했을 때만 기존 검색 → 비상 화법 체인을 탄다.

---

## 5. 라우팅 로그 — 한 줄로 판정

`resolveFact()` 가 결과와 **사유**를 함께 돌려준다. v2.9.3은 `null` 만 줘서
"소스가 없어서 안 한 건지 / 불렀는데 실패한 건지"를 구분할 수 없었다.

```
22:20 · Gemini 강지원 900ms
      🚫 신선도:환율
      📡 데이터API 성공: 환율

22:20 ✕ Groq 강지원 429
      🚫 신선도:환율
      📭 데이터API 실패: 환율 시간 초과

22:20 · Groq 서다현 400ms
      · 해당 없음(잡담)
```

| 표시 | 뜻 |
|---|---|
| 📡 | 데이터 API 성공 |
| 📭 (경고색) | 실패 — 사유까지 (`시간 초과`, `값 없음`, HTTP 오류) |
| (없음) | 미발동 — `해당 소스 없음` / `사진 첨부` |

측정 패널에도 `데이터API 4(실패 1)` 형태로 실패 건수를 함께 센다.

---

## 6. 검증

- 문장 21개 변형 전부 데이터 API 경로 진입 (v2.9.3 구멍 3건 + 미매칭 1건 해소)
- `resolveFact` 3상태 실측: `ok` / `fail(환율 시간 초과)` / `skip(해당 소스 없음)`
- 타임아웃 실패를 강제로 유발해 폴백 경로 확인 — 예외가 대화를 막지 않는다
- 실제 API 왕복: USD 1,411.27원(8/14) · JPY 100엔 887.52원 · EUR 1,632.42원 · 부산 26도
- 조사 처리 4개 도시 + 하늘 상태 3종 정확
- v2.9.3 회귀 없음 — Validator 5케이스, 서킷 3회 OPEN·복구, 대기 종료 4조건, 5인 라우팅
- 문법 오류 0

**실기기 확인 미완** (브라우저 확장 미연결): 한도 상황에서 Fact 말풍선이 실제로 뜨는지,
라우팅 로그의 📡/📭 표시. 다음에 429를 만나면 설정 → 🔀 최근 라우팅을 열어 확인하는 걸 권한다.

### 부수적으로 정리한 것

`DATA_SOURCES.fx.currencies[].code` 는 **쓰이지 않는 죽은 필드**였다 (URL은 키를 그대로 쓴다).
테스트에서 이 필드를 바꿔도 동작이 안 변해서 발견했다. 키가 곧 통화 코드가 되도록 정리했다 —
두 곳에 같은 값을 두면 언젠가 어긋난다.

---

## 7. 조정하는 법

| 원하는 것 | 방법 |
|---|---|
| 표현 추가 | `DATA_SOURCES[*].match` 에 한 줄 (신선도로도 자동 반영) |
| 통화 추가 | `DATA_SOURCES.fx.currencies` 에 키 하나 (키 = API 통화 코드) |
| Fact 보존 끄기 | `fetchFxFact`/`fetchWeatherFact` 의 `plainText` 를 비운다 |
| 데이터 API 타임아웃 | `DATA_SOURCES[*].timeoutMs` (기본 4000) |
