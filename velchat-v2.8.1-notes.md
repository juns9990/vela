# 벨챗 v2.8.1 — Gemini 신형 키 허용 + PC 창 모드 둥근 채팅방

v2.8.0 에서 이틀 만에 드러난 두 가지를 고친다. 둘 다 **"내가 너무 단정했다"**는 같은 종류의 실수다.

---

## 1. Gemini 키 형식 — 앱이 발급처보다 앞서가면 안 된다

### 무슨 일이 있었나

v2.8.0 은 네트워크를 쓰기 전에 키 형태를 검사해 **그 자리에서 실패로 돌려보냈다.**

```js
if (cfg.keyPattern && !cfg.keyPattern.test(key)) {
  return { ok: false, msg: `${cfg.name} 키 형태가 아닙니다 — sk-ant-... 으로 시작해야 합니다` };
}
```

Gemini 패턴은 `/^AIza[0-9A-Za-z_-]{10,}$/` 였다.
구글이 `AIza` 접두어가 없는 새 형식 키를 내보내기 시작하자,
**멀쩡히 동작하는 키를 앱이 거부했다.** 저장도 연결 테스트도 안 됐다.

당시 이 검사를 넣은 이유는 이랬다 —
"네트워크를 쓰기 전에 잡을 수 있는 오타가 제일 많다."
의도는 맞았지만 **판정 권한을 잘못 가져왔다.**
키가 유효한지는 발급처만 안다. 형식은 발급처가 언제든 바꿀 수 있고,
앱은 그걸 따라갈 방법이 없다. 접두어를 열거하기 시작하면
구글이 또 바꿀 때마다 같은 사고가 반복된다.

### 고친 방식 — 한 필드를 둘로 가른다

원래 `keyPattern` 하나가 성격이 다른 두 일을 겸하고 있었다.

- **QR·붙여넣기에서 "이건 Gemini 키다"라고 단정하는 일** → 틀리면 안 되니 **좁아야** 한다
- **사용자에게 "형태가 좀 이상한데요"라고 알려주는 일** → 막으면 안 되니 **넓어야** 한다

이 둘은 요구사항이 정반대다. 한 정규식으로 둘 다 하려니 어느 쪽도 제대로 안 됐다.

```js
gemini: {
  keyGuess: /^AIza[0-9A-Za-z_-]{20,}$/,   // 단정용 — 확실할 때만 맞는다
  keyShape: /^[A-Za-z0-9._-]{20,}$/,      // 참고용 — 접두어를 요구하지 않는다
}
```

- `keyGuess` — `parseKeyPayload()` 전용. QR 이나 붙여넣은 문자열에서 provider 를 자동으로 고를 때만 쓴다.
  신형 키는 여기 안 걸리지만 **문제되지 않는다.** 그때는 사용자가 provider 를 직접 고르면 된다.
  잘못 단정하는 것보다 단정하지 않는 게 낫다.
- `keyShape` — 최소한의 상식 검사뿐이다. 20자 이상, URL-safe 문자.
  공백이 섞였거나 너무 짧으면 걸리고, 그 외에는 다 통과한다.

Anthropic·Groq 도 같은 구조로 바꿨다. 그쪽 접두어는 안정적이지만,
**"형식 검사는 참고용"이라는 규칙에 예외를 두면 그 예외가 다음 사고 지점이 된다.**

### 경고는 실패했을 때만, 그것도 힌트로

```js
const shapeOdd = !!(cfg.keyShape && !cfg.keyShape.test(key));
// ... 형태와 무관하게 실제 호출을 보낸다 ...
if (res.ok) return { ok: true, msg: '연결 성공' };   // 서버가 받아줬으면 형태 얘기는 버린다
// ...
if (!out.ok && shapeOdd) {
  out.msg += '참고: 키 형태가 AIza... 와 달라 보입니다 — 다른 서비스의 키를 붙여넣지 않았는지 확인해보세요.';
}
```

세 가지가 요점이다.

1. **형태 검사가 호출을 막지 않는다.** `fetch` 는 무조건 나간다.
2. **성공하면 형태 경고를 통째로 버린다.** 서버가 받아준 키는 형태가 어떻든 옳다.
3. **실패했을 때만 덧붙인다.** 그때도 판정이 아니라 원인을 좁히는 힌트다 —
   Groq 키를 Gemini 칸에 붙여넣은 경우 이 한 줄이 바로 알려준다.

`keyPlaceholder` 도 `'AIza... (또는 새 형식 키)'` 로, 키 힌트에도
"형식이 달라도 그대로 붙여넣으면 됩니다"를 넣었다.

---

## 2. PC 창 모드 — 꽉 채우되, 둥글게

### v2.7.1 이 지나쳤던 부분

v2.7.1 은 "설치 창 = 채팅방"을 위해 여백·라운드·그림자를 **전부 0 으로** 밀었다.

```css
--stage-pad: 0px;  --shell-radius: 0px;  --shell-shadow: none;
```

방향은 맞았는데 정도가 지나쳤다. 창 가장자리에 픽셀이 딱 붙으니
**앱이 아니라 웹페이지를 전체화면으로 띄운 것처럼** 보인다.
카톡 PC 도 창을 꽉 쓰지만, 대화 영역은 얇은 여백 안에 둥글게 떠 있다.
그 얇은 틈이 "이건 창 안에 놓인 물건이다"라는 신호였던 것이다.

### 0 대신 "얇게"

```css
--stage-pad:   var(--win-pad, 8px);
--stage-pad-x: var(--win-pad, 8px);
--shell-radius: var(--win-radius, 18px);
--shell-shadow: var(--win-shadow, 0 8px 28px rgba(15,23,42,0.16), 0 2px 8px rgba(15,23,42,0.10));
```

여백에는 `body.chat-only` 의 방사형 그라데이션이 그대로 비친다.
테마를 바꾸면 이 테두리 색도 같이 바뀐다 — 별도 색을 안 쓴 이유다.

### 좌우 여백 변수가 새로 필요했다

기존 규칙은 이랬다.

```css
body.chat-only { padding: var(--stage-pad) 0; }   /* 위아래만 */
```

카드형에서는 이게 맞다. **좌우 여백은 `max-width` + 중앙정렬이 만들기** 때문이다.
그런데 창 모드는 `--shell-max: none` 이라 셸이 가로를 꽉 채운다 —
위아래만 띄우면 좌우는 여전히 창에 붙어 있다.

그래서 `--stage-pad-x` 를 새로 뒀다. 카드형에서는 `0px`(기존 동작 그대로),
창 모드에서만 `--win-pad` 를 받는다.

```css
body.chat-only { padding: var(--stage-pad) var(--stage-pad-x); }
```

### CONFIG 로 뺐다

```js
const WINDOW_STYLE = {
  padPx: 8,        // 창 가장자리와 셸 사이 여백 (권장 6~10)
  radiusPx: 18,    // 셸 모서리 반경
  shadow: '0 8px 28px rgba(15,23,42,0.16), 0 2px 8px rgba(15,23,42,0.10)'
};
```

`applyWindowStyle()` 이 이걸 `--win-pad` / `--win-radius` / `--win-shadow` 로 내린다.

**CSS 쪽에 `var(--win-pad, 8px)` 처럼 기본값이 박혀 있는 게 중요하다.**
JS 가 안 돌아도 모습은 똑같다. `applyWindowStyle()` 은 "CONFIG 로 조절 가능"을
위한 통로일 뿐, 화면이 이 함수에 의존하지 않는다. v2.7.1 에서
"CSS 가 주(主), JS 는 보강"으로 잡은 원칙을 그대로 이어간다.

**풀블리드 복귀** — `padPx: 0, radiusPx: 0` 이면 v2.7.1 동작으로 그대로 돌아간다.
이때 그림자는 자동으로 꺼진다. 여백도 라운드도 없으면 그림자가 보일 자리가 없기 때문이다.
둘 중 하나만 0 으로 두면 각각 독립으로 적용된다 (여백 0 + 라운드만, 같은 조합도 가능).

음수·비숫자가 들어오면 기본값으로 방어하고, 소수는 반올림한다.

### 좁은 창에서는 여전히 풀블리드

`@media (min-width: 560px)` 안에 있으므로, 폭이 그보다 좁은 창에서는 여백이 붙지 않는다.
**의도한 것이다.** 그 폭에서는 폰과 같아서 8px×2 가 아까운 가로폭을 깎아먹는다.
v2.7.1 에서 320px 까지 버티게 만든 작업(입력독·헤더 압축)을 그대로 살리려면
그 구간은 풀블리드가 맞다.

### 키보드가 뜨면 여백을 접는다 (기존 동작 유지)

```css
@media (min-width: 560px) {
  body.chat-only.kb-open { padding: 0; }
}
```

v2.7.2 에서 넣은 이 규칙이 새 좌우 여백까지 함께 접는다.
`padding: 0` 이 양축을 다 덮으므로 따로 손댈 게 없었다.
(PC 설치 창에는 소프트 키보드가 없어 `kb-open` 이 뜨지 않지만,
 태블릿 설치 앱에서는 이 경로가 살아 있다.)

---

## 3. 확인한 것

### 자동 테스트 — 31건 통과 (`test-v281.js`)

- **keyShape (5)** — 구형 AIza / 신형 접두어 없는 키 / 하이픈·언더바 포함 키가 모두 통과,
  너무 짧거나 공백 섞인 것만 경고 대상
- **keyGuess (7)** — AIza 긴 키만 Gemini 로 단정, 신형 키는 일부러 단정하지 않음,
  세 provider 샘플이 각각 **정확히 하나**로만 잡히는지(상호 배타성)
- **testApiKey 정책 (5)** — 조기 return 제거 확인, `fetch` 가 형태 검사보다 뒤,
  성공 시 경고 폐기, 실패 시에만 덧붙임
- **applyWindowStyle (6)** — 기본값 / 둘 다 0 → 풀블리드+그림자 off /
  한쪽만 0 → 독립 적용 / 빈 shadow / 음수·비숫자 방어 / 소수 반올림
- **CSS (8)** — 창 모드 여백·라운드·그림자 값, 양축 패딩,
  탭 모드 좌우 0 유지, media 경로와 `body.pwa-window` 경로 값 일치

### 회귀 — 37건 통과 (`regress.js`)

v2.7.2 키보드 · v2.7.1 창 모드 · v2.5 몰입/크기 · v2.3 언어 가드 ·
v2.2 사진 · v2.4 테마 · v2.7.0 키 UX · v2.8 provider 커버리지.

> 처음 돌렸을 때 2건이 실패했는데, **코드가 아니라 검사 쪽이 낡은 것**이었다 —
> `keyPattern` 을 `keyGuess`/`keyShape` 로 가른 이번 변경을 검사가 모르고 있었다.
> 단언을 새 이름으로 고치고, "옛 `keyPattern` 참조가 남아 있지 않은지" 검사를 하나 더 넣었다.

v2.8.0 테스트(23 + 21건)도 그대로 통과. `<style>` 중괄호 균형,
`<script>` 두 블록 파싱도 통과.

### 실기기·실호출은 못 했다

이 세션에서 Chrome 확장이 연결되지 않았고, **API 키가 없어 실제 호출은 하지 못했다.**
특히 **신형 Gemini 키의 실제 형태를 본 적이 없다** —
그래서 접두어를 추측해 넣는 대신 `keyShape` 를 넓게 열어두는 쪽을 골랐다.
어떤 형태가 오든 통과하고, 판정은 서버가 한다.

**남은 확인 목록**

1. 신형 Gemini 키 붙여넣기 → 저장·연결 테스트가 막히지 않는지 (이번 수정의 핵심)
2. 일부러 틀린 키 → 실패 메시지 뒤에 "참고: 키 형태가…" 가 붙는지
3. 구형 `AIza` 키 QR 스캔 → provider 가 자동으로 Gemini 로 잡히는지
4. 신형 키 QR 스캔 → 자동 판별은 안 되지만 provider 를 직접 고르면 정상 동작하는지
5. 설치 창 실행 → 얇은 여백 + 둥근 모서리 + 여백에 배경 그라데이션이 비치는지
6. 테마를 바꿔가며 → 여백의 테두리 색이 테마를 따라가는지
7. 설치 창을 560px 아래로 좁힘 → 풀블리드로 전환되는지
8. `WINDOW_STYLE.padPx = 0, radiusPx = 0` → v2.7.1 풀블리드로 복귀하는지
9. 브라우저 탭 → 기존 중앙 카드형이 그대로인지 (좌우 여백 0 유지)

---

## 손댄 곳

| 파일 | 내용 |
|---|---|
| `vela-boardroom-prototype.html` | `keyPattern` → `keyGuess`/`keyShape` 분리 · 형태 검사를 경고로 강등 · `WINDOW_STYLE` CONFIG · `--stage-pad-x` · `applyWindowStyle()` · 버전 2.8.1 |
| `vela-boardroom-sw.js` | 스탬프가 `BUILD_ID` 갱신 |
| `velchat-v2.8.1-notes.md` | 이 문서 |
