# 벨챗 v2.9.1 — 실시간 수치 신뢰성 수정

2026-08-17 · `vela-boardroom-prototype.html`

한 줄 요약: **"지금 값"을 묻는 질문은 무조건 검색하고, 검색을 못 하면 아는 척하지 않는다.**

---

## 1. 무슨 일이 있었나

"환율 알려줘"에 강지원이 **1,352원**이라고 답했다. 실제는 **1,415원**이었다.
학습 시점의 숫자를 현재값처럼 말한 것이다. 63원 차이지만, 문제는 금액이 아니다 —
**한 번 이런 답이 나오면 그 방의 모든 숫자를 못 믿게 된다.**

### 원인을 두 갈래로 나눠 봤다

재현해보니 `"환율 알려줘"` 자체는 v2.9에서도 검색 판정이 **true** 였다.
그런데도 사고가 났다는 건 원인이 판정 로직 바깥에 있다는 뜻이다.

| # | 원인 | v2.9 상태 |
|---|---|---|
| ① | **검색 가능한 키가 없었다** (Groq 단독 등) → 검색 자체가 불가능 | 이때 아무 방어가 없었다. 모델이 그냥 아는 숫자를 말한다 |
| ② | 신선도 주제인데 **트리거 사전에 빠진 말**이 있었다 | `날씨`, `코스피`, `기름값`, `박스오피스` 등이 안 걸렸다 |
| ③ | 검색이 붙어도 모델이 **기억 속 숫자를 쓸 수 있다** | "검색 결과가 우선"이라는 지시가 없었다 |
| ④ | 사후에 **왜 검색을 안 했는지 추적할 수단**이 없었다 | 라우팅 내역에 검색 정보가 없었다 |

v2.9.1은 네 가지를 다 막는다. 그중 **①이 진짜 근본 원인**이다 — 검색은 언제든 불가능할 수 있고,
그때 정직하게 말하는 것이 유일한 방어선이다.

### v2.9 에서 뚫려 있던 구멍 (재현 결과)

```
"코스피 오늘 어때"    → 검색 X   ← 구멍
"오늘 날씨 어때?"     → 검색 X   ← 구멍
"기름값 올랐어?"      → 검색 X   ← 구멍
"환율 알려줘"        → 검색 O   (키가 있었다면)
```

---

## 2. 신선도 필수 주제 강제 검색 (`FRESHNESS_TRIGGERS`)

CONFIG에 카테고리별 사전을 새로 뒀다. **메시지 무게와 무관하게** 검색을 건다 —
잡담처럼 짧은 "환율 얼마야"도 학습 시점 숫자를 말하면 안 되는 질문이기 때문이다.

| 카테고리 | 예시 단어 |
|---|---|
| 환율 | 환율, 달러값, 원달러, 엔화, 유로화 |
| 주가 | 주가, 코스피, 코스닥, 나스닥, 증시, 시총, 공모주 |
| 코인 | 비트코인, 이더리움, 코인 시세, 가상자산 |
| 시세 | 시세, 집값, 전세가, 분양가, 금값, 기름값, 유가, 전기요금 |
| 금리 | 금리, 기준금리, 대출금리, 이자율 |
| 날씨 | 날씨, 기온, 몇 도, 미세먼지, 태풍, 장마, 폭염 |
| 뉴스 | 뉴스, 속보, 오늘 이슈, 터졌, 발표했 |
| 순위 | 순위, 랭킹, 차트, 1위, 박스오피스, 베스트셀러 |
| 일정 | 개봉일, 출시일, 마감일, 접수 기간, 언제 나와 |
| 물가 | 물가, 인플레, 소비자물가, 최저임금, 실업률 |

**카테고리로 나눈 이유**는 진단 로그에 `신선도:환율` 처럼 그대로 찍기 위해서다.
빠진 주제가 보이면 해당 배열에 한 줄 추가하면 끝난다.

판정 순서도 바꿨다 — **신선도 사전이 메시지 무게보다 위**다.

```
① 검색 꺼짐 / 사진 첨부  → 검색 안 함
② 신선도 사전에 걸림     → 검색 (무게 무관) ★ 신설
③ 무게가 '안건'         → 검색
④ 무게가 '대화' + 트리거 → 검색
```

### 무게 승격 하나

신선도 질문이 `잡담`으로 판정되면 **`대화`로 올린다.**
잡담 무게는 소형 모델(fastModel)이 1~2문장으로 답하는데, 그 조합이 정확히
"기억 속 숫자를 툭 던지는" 사고가 나는 자리다. 검색 결과를 제대로 읽으려면 큰 모델과 문장 여유가 필요하다.

---

## 3. 검색을 못 할 때 — 단정 금지

**여기가 핵심 수정이다.** 검색 트리거를 아무리 촘촘히 해도, 키가 없으면 검색은 못 한다.
그때 모델이 아는 숫자를 말해버리면 결국 같은 사고다.

### 공통 프롬프트에 상시 규칙 추가 (`COMMON_RULES`)

```
[실시간 수치 — 모르면 단정하지 마라 · 가장 자주 사고가 나는 자리]
- 환율·주가·코인·금리·시세·집값·기름값·날씨·물가·순위처럼 시간에 따라 변하는 값이 있다.
  이런 건 네가 배운 시점의 숫자이지 지금 값이 아니다.
- 검색이 붙지 않은 턴에서 이런 질문을 받으면 기억 속 숫자를 현재값처럼 말하지 마라.
  "환율 1,352원이에요" 처럼 단정하는 순간, 실제와 다르면 방 전체를 못 믿게 된다.
- 대신 정직하게: "지금 정확한 수치는 확인을 해봐야 해요. 찾아볼까요?"
- 과거 지식으로 말할 때는 시점을 반드시 붙인다 — "작년 기준으로는", "제가 마지막으로 본 시점엔".
  시점 없이 던진 숫자는 전부 현재값으로 읽힌다. 그게 사고의 원인이다.
- 반대로 안 변하는 것(개념·구조·계산법)은 자신 있게 말해라.
  이 규칙은 "겁먹어라"가 아니라 "변하는 값과 안 변하는 값을 구분하라"는 뜻이다.
```

### 그리고 그 턴에만 강한 지시가 따로 내려간다

신선도 질문인데 검색이 안 붙은 턴에는 이게 추가된다:

> 지금 묻는 건 환율처럼 **시시각각 바뀌는 값**인데, 이번 턴에는 웹 검색이 붙지 않았다.
> 그러니 네가 아는 숫자를 현재값처럼 말하면 절대 안 된다. 그 숫자는 이미 틀렸을 가능성이 높다.
> "지금 정확한 수치는 확인이 필요하다"고 네 말투로 정직하게 말하고, 원하면 찾아봐 줄지 물어라.
> 과거 값을 언급할 거면 반드시 시점 표시를 붙여라.
> 숫자 대신 흐름·구조·판단 기준을 말하는 건 얼마든지 좋다.

마지막 줄이 중요하다. **입을 막는 게 목적이 아니다** — 숫자만 빼고 나머지는 평소처럼 하라는 것이다.

---

## 4. 검색이 붙었을 때 — 검색 결과 우선

모델이 검색 결과를 받고도 "아는 숫자"를 쓰는 일이 있다.
그러면 본문 숫자와 아래 출처 링크가 어긋나서 **오히려 더 나쁘다.** 그래서 세 줄을 추가했다:

> **네 기억과 검색 결과가 다르면 무조건 검색 결과가 맞다.** 기억 속 숫자를 덮어써라.
> 답변에 쓰는 수치는 검색해서 본 출처의 값과 정확히 일치해야 한다. 반올림해서 바꾸거나 어림잡아 고치지 마라.
> (신선도 질문이면) 검색으로 확인한 **현재 값**을 기준 시점과 함께 말해라 (예: "오늘 기준").

---

## 5. 진단 로그 — "왜 검색을 안 했지?"

설정 → 최근 라우팅에 **검색 판정 한 줄**이 붙는다.

```
17:22 · Gemini  강지원   2400ms
      🔍 신선도:환율

17:22 · Groq    강지원    700ms
      🚫 신선도:환율 → 검색 가능한 키 없음      ← 빨간색으로 뜬다

17:22 · Groq    서다현    400ms
      · 해당 없음(잡담)
```

| 표시 | 뜻 |
|---|---|
| 🔍 | 검색이 실제로 붙었다 |
| 🚫 (경고색) | **걸고 싶었는데 못 했다** — 키가 없거나 사진이 붙었거나 |
| · | 검색할 질문이 아니었다 |

사유 문자열은 `신선도:환율`, `무게:안건`, `단어:최신`, `해당 없음(잡담)`, `사진 첨부`, `검색 꺼짐` 중 하나다.
🚫가 계속 보이면 **Gemini 키를 넣어야 한다는 신호**다.

---

## 6. 검증 결과

v2.9에서 뚫려 있던 구멍이 전부 막혔다:

```
"환율 알려줘"        대화   검색O  신선도:환율
"코스피 오늘 어때"    대화   검색O  신선도:주가      ← v2.9 에서는 X
"오늘 날씨 어때?"     대화   검색O  신선도:날씨      ← v2.9 에서는 X
"기름값 올랐어?"      대화   검색O  신선도:시세      ← v2.9 에서는 X
"비트코인 지금 얼마?"  안건   검색O  신선도:코인
"박스오피스 1위 뭐야"  대화   검색O  신선도:순위
"최저임금 얼마야"     대화   검색O  신선도:물가

"점심 뭐 먹지"       잡담   검색X  해당 없음(잡담)   ← 잡담은 그대로 안 한다
"ㅋㅋ 배고프다"       잡담   검색X  해당 없음(잡담)
"그 영화 재밌었어"    대화   검색X  해당 없음(대화)
사진 첨부 + 안건            검색X  사진 첨부        ← 사진이 우선
```

- 지시문 3종(검색 붙음 / 검색 못 붙음 / 평범한 잡담) 실제 출력 확인 — 잡담에는 아무 것도 늘지 않는다
- 진단 로그 렌더 확인 — 🔍 / 🚫(경고색) / · 세 상태 모두
- 인라인 스크립트 문법 오류 0

**아직 실기기 확인을 못 했다** (브라우저 확장 미연결):
실제 키로 "환율 얼마야"를 물었을 때 검색이 붙는지, 키가 없을 때 정말로 단정을 피하는지.
**이 두 가지는 첫 실행 때 꼭 확인하는 게 좋다** — 3절은 프롬프트 규칙이라 모델 순응도에 달려 있다.

---

## 7. 비용 영향

신선도 질문에만 검색이 늘어난다. 잡담·리액션은 그대로 0이다.
"환율 얼마야" 같은 짧은 질문이 하루 몇 건 늘어나는 수준이라 **Gemini 무료 한도 안에서 흡수된다.**
무게 승격(잡담→대화)도 해당 질문에만 적용돼 체감 비용 변화는 거의 없다.

---

## 8. 조정하는 법

| 원하는 것 | 방법 |
|---|---|
| 신선도 주제 추가 | `FRESHNESS_TRIGGERS` 의 해당 카테고리 배열에 한 줄 |
| 카테고리 신설 | `FRESHNESS_TRIGGERS` 에 키 하나 추가 (진단 로그에 자동 반영) |
| 강제 검색 끄기 | 해당 카테고리 배열을 비운다 |
| 검색 전체 off | `SEARCH_RULES.enabled = false` (단정 금지 규칙은 그대로 남는다) |
