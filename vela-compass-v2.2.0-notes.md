# Vela Compass v2.2.0 — 상세 변경 노트

> 2026-08-14 · 대상 파일: `vela-boardroom-prototype.html` (단일 파일 앱)
> 이전 버전: v2.1.0 (단톡방 전환)

---

## 1. 입력창 하단 고정 — 문서 스크롤 → 고정 셸

### 무엇이 문제였나
v2.1 까지는 페이지 전체(`body`)가 스크롤됐다. 대화가 길어지면 입력창이 문서
아래쪽으로 밀려 화면 밖으로 사라지고, 말을 걸려면 매번 맨 아래까지 스크롤해야 했다.

### 어떻게 바꿨나
`body.chat-only` 를 **고정 높이 flex 셸**로 바꾸고, 스크롤을 메시지 영역
한 곳에만 남겼다.

```
html.chat-lock          overflow: hidden          ← 문서 스크롤 완전 차단
└ body.chat-only        position: fixed / flex column / height: var(--app-h)
  ├ .masthead           flex: 0 0 auto            ← 상단 고정
  ├ .issue-strip        flex: 0 0 auto
  ├ main                flex: 1 1 auto / min-height: 0
  │ └ .chat-screen      flex: 1 1 auto / min-height: 0
  │   ├ .chat-header    flex: 0 0 auto
  │   ├ .chat-participants
  │   ├ .chat-messages  flex: 1 1 auto / overflow-y: auto   ★ 여기만 스크롤
  │   └ .chat-dock      flex: 0 0 auto            ← 하단 고정
  │     ├ .chat-jump    (배지 · position: absolute, bottom: 100%)
  │     ├ .chat-routing
  │     ├ .chat-attach-strip
  │     └ .chat-input-bar
  └ .colophon           display: none (고정 셸에는 문서 하단이 없다)
```

- `min-height: 0` 이 핵심이다. flex 아이템의 기본 `min-height: auto` 때문에
  내용이 많아지면 컨테이너가 부풀어 스크롤이 셸 밖으로 새어나간다.
- 콜로폰(MADE BY JUNS)은 고정 셸에서 갈 자리가 없어 숨기고, 대신
  이슈 스트립 우측에 `MADE BY JUNS · 2026` 으로 옮겼다.
- `.btn-to-top`(맨 위로 버튼)도 문서 스크롤 전용이라 chat-only 에서는 숨겼다.

### 모바일 키보드 (visualViewport)
키보드가 올라오면 `visualViewport.height` 가 줄고, iOS 는 추가로 `offsetTop`
만큼 화면을 밀어올린다. 두 값을 CSS 변수로 넘겨 셸을 실시간으로 맞춘다.

```js
function syncViewport() {
  const vv = window.visualViewport;
  root.style.setProperty('--app-h', (vv ? vv.height : innerHeight) + 'px');
  root.style.setProperty('--vv-top', (vv ? vv.offsetTop : 0) + 'px');
}
```

- `visualViewport` 의 `resize` · `scroll` 양쪽에 물려 키보드가 올라오는
  **애니메이션 도중에도** 계속 따라간다.
- resize 때 바닥에 붙어 있던 상태면 다시 바닥으로 — 키보드가 마지막 메시지를
  덮지 않게.
- 입력창 `focus` 시에도 250ms 뒤 한 번 더 바닥을 잡는다.
- `visualViewport` 없는 구형 브라우저는 `window.innerHeight` 로 자동 폴백.

### 아이폰 안전영역
```css
body.chat-only .chat-input-bar {
  padding-bottom: calc(8px + env(safe-area-inset-bottom, 0px));
}
```
홈 인디케이터 위로 띄운다. 키보드가 올라오면 iOS 가 이 값을 0 으로 주므로
키보드 위에서는 여백이 자동으로 사라진다 — 별도 분기가 필요 없다.

---

## 2. 자동 스크롤 + "새 메시지 ↓" 배지

### 정책
| 상황 | 동작 |
|---|---|
| 바닥에 붙어 있음 + 새 메시지 | 따라 내려간다 |
| 위로 올려 읽는 중 + 남의 메시지 | **스크롤 안 함** + 배지 표시 |
| 위로 올려 읽는 중 + 내 메시지 | 무조건 바닥으로 (배지 해제) |
| `typing` 인디케이터 | 새 메시지로 세지 않음 |
| 세션 복원 (`restore: true`) | 메시지마다 스크롤하지 않고 마지막에 한 번만 |

- 판정 기준: `scrollHeight - scrollTop - clientHeight <= 72px` 이면 "바닥"
- 배지는 누적 개수를 보여준다 (`새 메시지 3 ↓`). 클릭하면 바닥으로 복귀.
- 배지 위치는 `.chat-dock` 기준 `bottom: 100%` — 도크 높이(첨부 미리보기
  유무 등)가 변해도 항상 입력창 바로 위에 붙는다.

### 자동 스크롤이 사용자 조작으로 오인되는 문제
부드러운 스크롤은 애니메이션 동안 `scroll` 이벤트를 계속 발생시킨다.
그 사이에 메시지가 하나 더 도착하면 "아직 바닥이 아님"으로 읽혀
따라가던 상태가 풀려버렸다 (개발 중 실측으로 발견).

→ 프로그램 스크롤 직후 짧은 보호 구간(`programmaticScrollUntil`)을 두되,
**시간만으로 판단하지 않는다.** `wheel` · `touchstart` · `touchmove` ·
`mousedown` · 스크롤 키가 들어오면 보호 구간을 즉시 해제한다.
이 이벤트들은 `scroll` 보다 먼저 오기 때문에, 자동 스크롤 직후 사용자가
위로 올린 경우도 정확히 잡아낸다.

### 그 밖의 보정
- **사진 로드 후 재고정**: 이미지는 로드가 끝나야 높이가 확정되므로,
  `img.load` 시점에 바닥을 한 번 더 잡는다.
- **숨겨진 탭**: 브라우저는 백그라운드 탭에서 부드러운 스크롤을 실행하지
  않는다. `visibilitychange` 로 돌아왔을 때 즉시 바닥을 다시 잡는다.

---

## 3. 사진 업로드

### 첨부 경로 3가지
1. 입력창 옆 `+` 버튼 → 파일 선택 (`multiple`)
2. 입력창에 **붙여넣기** (⌘/Ctrl+V) — 클립보드 이미지
3. 채팅 영역에 **드래그 앤 드롭**

### 리사이즈 · 압축 (`IMAGE_RULES`)
| 항목 | 값 |
|---|---|
| 긴 변 최대 | 1280px (비율 유지) |
| 형식 | 항상 JPEG (PNG 는 base64 가 3~5배 커진다) |
| 품질 | 0.82 |
| 한 메시지당 | 최대 4장 |
| 원본 상한 | 20MB (넘으면 거절 — 디코딩에서 브라우저가 죽는다) |

- 투명 PNG → JPEG 변환 시 검게 나오는 문제를 막으려고 흰 배경을 먼저 깐다.
- 실측: **5.1MB PNG(3000×2000) → 16KB JPEG(1280×853)**

### 전송 전 미리보기
- 62px 썸네일 + 우상단 `×` 삭제 버튼 (장별 개별 삭제)
- 옆에 `2장 · 27KB · 최대 1280px 로 줄여서 보냅니다` 안내
- 전송하면 자동으로 비워진다

### 멀티모달 API
내부 표준은 **Anthropic 블록 형식**이고, provider 별로 변환한다.

```
buildConversationHistory()  → content: string | [{type:'image'|'text'}]
        ↓
normalizeMessagesForProvider(messages, provider)
  · anthropic          → 그대로
  · vision 지원 OpenAI 호환 → image_url 형식으로 변환
  · vision 미지원(Groq) → 이미지 블록 제거, 텍스트만
```

- 연속 같은 role 병합(Anthropic 요구사항)이 블록 배열에서도 깨지지 않게
  `toContentBlocks()` 로 정규화 후 concat.
- `runPersonaTurn` 의 지시문 덧붙이기도 배열/문자열 양쪽 분기.

**비용 가드** — 사진을 전부 다 보내면 턴이 쌓일수록 요청이 기하급수로 커진다.

- `historyTurns: 2` — 최근 **사진 올린 턴** 2개만 실어 보낸다.
  (최근 "사용자 턴"이 아니다. 사진 하나 올리고 몇 마디 주고받으면
  사진이 바로 밀려나기 때문에 사진 턴 기준으로 센다.)
- `historyMaxImages: 4` — 그중에서도 총 장수 상한
- 밀려난 사진은 `(사진 N장 첨부됨 — 이전 대화라 이미지는 생략)` 텍스트로 남아
  문맥은 유지된다.

### 이미지 미지원 모델 안내
`LLM_CONFIG[provider].vision` 플래그로 판단한다 (anthropic: true, groq: false).

- 첨부 스트립 안내 문구가 경고색으로 바뀜:
  `Groq은 사진을 못 봅니다 — 방에만 남고 답변엔 반영되지 않아요`
- 전송 시 방 알림 칩:
  `Groq은 사진을 볼 수 없습니다 — … (설정에서 Anthropic 으로 바꾸면 사진을 보고 답합니다)`
- 페르소나 지시문도 바뀐다: *"너는 그 사진을 볼 수 없다. 본 척하지 말고
  궁금한 걸 되묻거나 글로 설명해달라고 해라."* → 환각 방지
- 설정에서 provider 를 바꾸면 안내가 즉시 갱신된다.

### 세션 저장 용량 가드 (`SESSION_STORE`)
localStorage 는 보통 5MB. 사진 base64 가 들어가면 금방 찬다.

`saveSessions()` 가 저장 **전에** 크기를 재고, 예산(3.4MB)을 넘으면:
1. 오래된 세션(배열 뒤쪽)의 사진 원본부터 제거 — 대화 내용은 그대로
2. 그래도 넘치면 오래된 세션 자체를 잘라냄
3. 그럼에도 quota 예외가 나면 catch 에서 단계적으로 더 회수

**중요**: 사진을 떼어낼 때 반드시 **사본**에서 작업한다.
`record.messages` 는 화면이 보고 있는 `state.messages` 와 같은 객체라,
그대로 지우면 저장하다가 화면의 사진까지 사라진다.

실측: 7.39MB(사진 72장) → 3.09MB 저장, 세션 12개 전부 보존,
현재 세션 사진 유지, 오래된 세션 사진만 정리, 메모리 원본 무사.

사진이 지워진 자리는 `사진 N장 · 저장 공간 확보를 위해 원본이 정리되었습니다`
로 표시된다. 마크다운 내보내기는 base64 대신 `_(사진 N장 첨부)_` 로 표기.

---

## 4. 유머 톤

### 공통 규칙 (`HUMOR_RULES`)
- 농담·드립·가벼운 개그 **권장**. ㅋㅋ ㅋㅋㅋ ㅎㅎ 자연스럽게, 재밌으면 길게.
- **동료끼리 서로 놀리기 허용** — "또 시작이네ㅋㅋ", "정민철 표정 안 봐도 알겠다".
  놀림당하면 발끈하거나 받아치는 것도 좋다.
- **사용자 비하 금지** — {chair}의 아이디어·질문·수준을 웃음거리로 삼지 마라.
  *"{chair}와 함께 웃는 건 좋고, {chair}를 두고 웃는 건 안 된다."*
- 외모·출신·성별·나이·장애·집단 소재 농담 금지.
- 억지로 웃기지 마라 — 매 발언마다 개그를 넣으면 피곤하다.

### 개인별 개그 스타일 (`persona.humor` → `humorStyleBlock()`)
| 인물 | 스타일 |
|---|---|
| 서다현 | **엉뚱한 드립** — 이상한 데로 비유. "완전 편의점 신상 같은 느낌인데?" |
| 이준호 | **현실 자학개그** — 자기가 말아먹은 얘기. "나 예전에 이거 하다 3주 날렸는데ㅋㅋ" |
| 정민철 | **건조한 팩트 개그** — 표정 없이 숫자 팩폭. "월 3개 팔면 12년 뒤 손익분기입니다" |
| 전문가석 | **현장 전문용어 개그** — "이거 현장에서 하면 반장님한테 맞습니다ㅋㅋ" |

각 프롬프트 끝에 *"동료 셋은 각자 다른 방식으로 웃긴다. 남의 개그 스타일을
따라 하지 마라."* 를 붙여 톤이 뭉개지지 않게 했다.

---

## 5. 깨진 글자 (타 문자권 혼입) 수정

### 프롬프트 강화 (`COMMON_RULES`)
`[한국어 출력 — 위반 시 응답 무효 · 가장 중요]` 로 승격하고 금지 대상을
**문자 체계 이름으로 구체적으로 명시**했다 — 아랍 문자, 히브리 문자,
키릴 문자, 태국 문자, 데바나가리, 그리스 문자, 한자, 일본어 가나,
성조 부호 라틴 문자(ă â đ ê ô ơ ư ő ű).

> "이 중 하나라도 섞이면 그 응답은 통째로 버려진다."
> "확실하지 않은 단어는 쓰지 말고, 아는 한국어로 풀어 써라."

### 출력 필터 — 블랙리스트 → 화이트리스트
기존 `sanitizeKorean()` 은 "아는 것만" 지우는 블랙리스트라, 처음 보는
문자권이 나오면 그대로 화면에 떴다. 기존 정화 로직 **같은 자리(8.5단계)**에
화이트리스트를 추가해 반대로 뒤집었다 — 목록에 없는 문자는 무조건 버린다.

**허용 범위** (`ALIEN_CHAR_RE` 의 여집합):

| 분류 | 범위 |
|---|---|
| 한글 | `AC00-D7A3` (완성형) + `3131-3163` (ㅋㅋ ㅎㅎ ㅠㅠ) |
| ASCII | `0020-007E` + 탭/줄바꿈 |
| 문장부호 | `2010-205E` (— … ‘’ “”), `00B7` (·) |
| 통화·기호 | ₩ € £ ¥ ° ± × ÷ |
| 화살표·수학 | `2190-21FF`, `2200-22FF` (→ ≈ ≠ ≤ ≥) |
| 도형·기호 | `25A0-25FF`, `2600-27BF`, `2B00-2BFF` (■ ● ★ ✓) |
| 이모지 | `1F000-1FAFF` + ZWJ · 변형 선택자 (조합 이모지 👨‍💻 🇰🇷 보존) |

### 원인 추적
`logAlienChars()` 가 **`sanitizeKorean()` 진입 시점에, 모델이 준 원문 기준으로**
기록한다. 앞선 한자 매핑·문자권 제거 단계가 먼저 지워버리면 무엇이 나왔는지
알 수 없게 되어 "어느 모델이 범인인가"에 답할 수 없기 때문이다.

```
[compass:필터] 허용 범위 밖 문자 24개 발견 — anthropic/claude-sonnet-4-6 · maker
                                             · 제거: م ر ح ب ا П р и в е т 製 作 …
```

기록 항목: `provider` · `model` · `persona` · `where` · 제거된 문자 ·
**코드포인트(U+0645 …)** · 개수 · **원문 전체** · 타임스탬프

콘솔에서 `window.__compassFilterLog` 로 최근 50건 누적 조회 가능
(provider/model 별로 구분되므로 어느 모델이 뱉는지 바로 확인된다).

**실측 결과**
```
입력: 사진 잘 봤어요 ㅋㅋㅋ مرحبا بالعالم 원가는 1,200원, Привет 마진 65%.
      製作 난이도 낮음. สวัสดี 😂 בדיקה
출력: 사진 잘 봤어요 ㅋㅋㅋ 원가는 1,200원, 마진 65%. 제작 난이도 낮음. 😂
```
아랍어·키릴·태국어·히브리어 제거 · 한자는 한국어로 변환(製作→제작) ·
ㅋㅋㅋ / 이모지 / 숫자 / 문장부호 보존.

---

## 검증 (실제 브라우저 실측)

| 항목 | 결과 |
|---|---|
| 입력창 하단 고정 | 입력창 bottom == 창 높이, 문서 `scrollY` 0 고정 |
| 메시지 영역만 스크롤 | `body: overflow hidden` / `.chat-messages: overflow-y auto` |
| 바닥 붙어있을 때 새 메시지 | 따라 내려감 (gap 0) |
| 위로 올려 읽는 중 | 스크롤 안 움직임 + 배지 표시 |
| 배지 누적 | `새 메시지 3 ↓` |
| typing 제외 | 카운트 증가 안 함 |
| 내 메시지 | 읽는 중이어도 강제로 바닥 |
| 배지 클릭 | 바닥 복귀 + 배지 사라짐 |
| 사진 압축 | 5.1MB PNG → 16KB JPEG (1280×853, 비율 유지) |
| 첨부 상한 | 4장에서 정확히 멈춤 |
| Anthropic 이미지 전송 | base64 블록, `media_type: image/jpeg` |
| Groq | 이미지 블록 제거, base64 유출 없음, 안내 문구 |
| 사진 문맥 유지 | 잡담이 껴도 최근 사진 2턴 유지, 그 이전은 자리표시자 |
| 저장 용량 가드 | 7.39MB → 3.09MB, 세션 12개 보존, 메모리 원본 무사 |
| 출력 필터 | 4개 문자권 제거, ㅋㅋㅋ·이모지 보존 |
| 추적 로그 | provider/model/persona 구분 기록 |
| 유머 프롬프트 | 4인 각기 다른 스타일 주입 확인 |
| 모달 | 고정 셸 안에서도 전체 화면 오버레이 정상 |

---

## 변경 파일

- `vela-boardroom-prototype.html` — 전부 여기 (단일 파일 앱)
- `vela-boardroom-sw.js` — 스탬프가 BUILD_ID 갱신
- `vela-compass-v2.2.0-notes.md` — 이 문서 (신규)

## 유지보수 메모

동작 조정은 여전히 CONFIG 블록 한 곳에서 끝난다. v2.2 에서 추가된 항목:

- `IMAGE_RULES` — 사진 크기·품질·장수·히스토리 상한
- `SESSION_STORE` — 저장 예산 · 보관 세션 수
- `HUMOR_RULES` / `CORE_PERSONAS[].humor` / `EXPERT_HUMOR` — 유머 톤
- `ALIEN_CHAR_RE` — 허용 문자 범위
- `LLM_CONFIG[provider].vision` — 이미지 지원 여부

v2.0 회의 기능(라운드·회의록·모드 선택)은 `FEATURES` 플래그로 여전히 꺼둔 상태이며
코드는 그대로 남아 있다.
