# 벨챗 v2.9.0 — 5인 분야 담당 체계 + 웹 검색 연결

2026-08-17 · `vela-boardroom-prototype.html` (단일 파일 앱)

한 줄 요약: **방에 다섯 명이 앉았고, 각자 맡은 분야가 생겼다. 그리고 안건에는 진짜로 검색해서 답한다.**

---

## 1. 무엇이 달라졌나

### 고정 3인 → 고정 5인 (여4 · 남1)

| id | 이름 | 담당 분야 | 색 |
|---|---|---|---|
| `director` | 서다현 | 문화 · 예술 · 트렌드 · 마케팅 · 여행 | `#f5b942` 앰버 |
| `maker` | 이준호 | 기술 · IT · 제조 · 개발 · 자동차 | `#5eead4` 틸 |
| `analyst` | **강지원** | 경제 · 금융 · 부동산 · 창업 · 투자 | `#8b5cf6` 바이올렛 |
| `care` | **한소이** | 생활 · 건강 · 의료 · 요리 · 육아 | `#4ade80` 그린 |
| `anchor` | **윤채린** | 법 · 행정 · 시사 · 교육 · 스포츠 | `#38bdf8` 스카이 |

- 서다현·이준호는 말투를 그대로 유지했다. 이준호에게는 **"방의 유일한 남자" 자학 포지션**이 붙었다.
- 정민철은 명단에서 빠졌다. 담당 분야(숫자·리스크)는 강지원이 이어받았고,
  **지난 대화 속 정민철 발언은 이름·색·목소리까지 그대로 남는다** (아래 6절).

### 여성 4인이 서로 안 겹치게

| | 서다현 | 강지원 | 한소이 | 윤채린 |
|---|---|---|---|---|
| 온도 | 텐션 최고 | 쿨 | 따뜻함 | 중립·단정 |
| 문장 | 길고 빠름 | 아주 짧음 | 흐르듯 | 짧고 구조적 |
| 어미 | "~인데?!" | "~예요", "~죠" | "~해요~" | "~입니다" |
| 감탄사 | 오!! 헐 | (없음) | 어머 아이고 | (없음) |
| 이모지 | ✨🔥👀 | 없음 | 🙂😭☺️ | 없음 |
| 웃음 | ㅋㅋㅋ 길게 | ㅋ 하나 | ㅎㅎ ㅠㅠ | 없음 |
| 개그 | 엉뚱한 비유 | 건조한 숫자 팩폭 | 생활 공감·자기 실패담 | 시크한 클로징 한 줄 |
| 존댓말 | 반말 섞임 | 반존대 | 다정한 존댓말 | 격식 존댓말 |

말투 고정에 가장 크게 작용하는 건 규칙이 아니라 **예시 대사(few-shot) 3개**다. 신규 3인 모두 3개씩 넣었다.

### TTS 5보이스 차등

| 페르소나 | pitch | rate |
|---|---|---|
| 서다현 | 1.25 | 1.08 |
| 한소이 | 1.16 | 0.92 |
| 강지원 | 1.06 | 1.02 |
| 윤채린 | 0.98 | 1.14 |
| 이준호 | 0.85 | 0.95 |
| (전문가) | 0.95 | 1.00 |
| (정민철·아카이브) | 1.00 | 0.90 |

인접한 두 사람이 피치·속도 **둘 다** 가깝지 않게 배치했다. 서다현↔한소이는 피치가 0.09 차이지만 속도가 0.16 벌어진다.
`VOICE_PERSONA_ORDER` 도 확장해서, 기기에 한국어 음성이 여러 개면 다섯 명에게 서로 다른 실제 보이스가 자동 배정된다.

---

## 2. 라우팅 — 안건은 담당자, 잡담은 로테이션

같은 점수식에 **배율만 다르게** 먹여 두 성격을 만든다 (`DOMAIN_RULES`).

```
안건·대화 : 키워드 ×1.4 + 담당자 +1.8 + 랜덤 1.2 − 최근발언 1.6   → 담당자가 이긴다
잡담·리액션: 키워드 ×0.56           + 랜덤 1.2 − 최근발언 2.4   → 로테이션이 이긴다
```

- **담당자**는 키워드가 2개 이상 걸린 사람 중 1위. 1개만 걸린 건 우연일 수 있어 담당자로 치지 않는다.
- 잡담 전원 등판은 `DOMAIN_RULES.smalltalkAll = true` 한 줄 (기본 off).
- 연쇄 반응(30%), 무게 판정, 응답 인원 상한(기본 2 / 하드 3)은 **그대로**다.
  → 인원이 늘어도 **메시지당 API 콜 수는 늘지 않는다.**

### 놓치면 비용이 오르는 보정 하나

`countKeywordHits` 는 사전 전체를 훑는다. 사전이 3벌 → 5벌이 되면 같은 문장의 적중 수가 자연히 올라가고,
`적중 3개 + 물음표 = 안건` 규칙이 과하게 걸려 **잡담이 안건으로 오판**된다.
오판 = 큰 모델 + 2명 + 웹 검색이 붙는다 = 그대로 비용이다.

그래서 판정 기준을 **"1위 담당자의 적중 수"**(`topDomainHits`)로 바꿨다. 사전 크기와 무관해진다.

```js
if (len >= 80 || sentences >= 3 || (topHits >= 2 && hasQuestion)) return 'serious';
```

### 실제 판정 결과 (헤드리스 검증)

```
"전세 대출 금리 지금 어때?"          안건  검색O  담당=강지원 → 강지원, 한소이
"계약서에 특약 없으면 누구 책임이야?"  안건  검색O  담당=윤채린 → 윤채린, 한소이
"전기차 배터리 교체 비용 얼마나 들어?" 안건  검색O  담당=이준호 → 이준호, 강지원
"아이가 열이 나는데 병원 가야 할까요?" 안건  검색O  담당=한소이 → 한소이, 윤채린
"이번 주말에 전시 볼 만한 거 있어?"    대화  검색X  → 서다현
"ㅋㅋ 배고프다"                       잡담  검색X  → (1명)

잡담 8번 연속 → 강지원 · 한소이 · 서다현 · 이준호 · 윤채린 · 강지원 · 한소이 · 서다현
안건 8번 연속 → 강지원 × 8 (담당자가 흔들리지 않는다)
```

---

## 3. 웹 검색 — Gemini 구글 검색 그라운딩 연결

### 조사 결과 (2026-08 기준)

| | Gemini | Anthropic |
|---|---|---|
| 활성화 | `tools: [{ google_search: {} }]` | `web_search_20250305` (v2.8부터 있었음) |
| 왕복 | **없음** — 같은 `generateContent` 호출 안에서 끝난다 | 없음 |
| 스트리밍 | 동작 | 동작 |
| 출처 | `groundingMetadata.groundingChunks[].web` | `citations` / `citations_delta` |
| 무료 | Gemini 3.x 월 5,000건 → 이후 약 $14/1k · 2.5 Flash 1,500 RPD → 이후 $35/1k | 없음 (건당 과금) |

→ **Gemini 를 1순위로 붙였다.** 무료 티어가 있고 왕복이 없다.

### 언제 켜지나 (`SEARCH_RULES`)

- **안건** → 무조건 켠다. 여기서 지어낸 수치가 나오면 방 전체의 신뢰가 무너진다.
- **대화** → 트리거가 걸릴 때만 (`최신`, `시세`, `얼마`, `환율`, `금리`, `규정`, `개정`, `뉴스`, `순위` …).
- **잡담·리액션** → **절대 안 켠다.** 점심 메뉴 얘기에 구글을 부를 이유가 없다.
- **사진이 붙은 메시지** → 안 켠다. 사진 보는 곳으로 가는 게 우선이고, 둘을 동시에 요구하면 갈 곳이 사라진다.
- 한 메시지에 두세 명이 답해도 **검색은 첫 사람 한 번뿐**(`maxPerTurn: 1`). 두 번째 사람은 그 결과를 보고 말한다.

### Groq 을 쓰고 있어도 검색된다

Groq 은 검색을 못 한다. 그래서 `resolveRoute` 에 `needsSearch` 를 넣어 **그 턴만 Gemini 로 우회**한다.
Gemini 키가 없으면 우회하지 않고 검색 없이 그냥 간다 — **검색을 못 한다고 대화가 멈추는 건 최악이다.**
자동 모드에서 검색 가능한 키가 하나도 없을 때도 마찬가지로 일반 후보로 되돌아간다.

### 지어낸 출처 금지 규칙과의 정합

- 화면에 뜨는 출처는 **API 메타데이터에서 온 것만**이다. 모델이 본문에 쓴 URL 은 링크 목록에 들어가지 않는다.
- `COMMON_RULES` 의 기본값을 **"이 방은 검색이 없는 상태"**로 바꿨다. v2.8 까지는 검색이 안 되는 provider 에도
  "적극적으로 검색하라"가 항상 들어가서 모델에게 헛된 기대를 줬다.
- 검색이 켜진 턴에만 별도 지시가 내려간다. 그 마지막 줄이 가장 중요하다:
  > 검색해도 안 나온 수치는 여전히 말하지 마라. **검색이 붙었다고 지어내도 되는 게 아니다.**

### 구글 검색 제안 위젯 — 무수정, 대신 접어둔다

구글 그라운딩 사용 요건상 제공된 위젯(`searchEntryPoint.renderedContent`)은 **수정하거나 다른 것으로 대체할 수 없다.**
그래서 검색어 칩으로 갈음하지 않고 **위젯 원문을 그대로** 넣는다.

다만 단톡방 한가운데 검색창이 펼쳐져 있으면 몰입이 깨지므로, `<details>` 로 감싸 **"출처 보기 · N건"** 탭으로 접어뒀다.
감싸기만 할 뿐 위젯 내부는 한 글자도 건드리지 않는다.

**그리고 Shadow DOM 안에 심는다.** 이유가 있다 — 구글 위젯에는 `<style>` 이 함께 오고 그 선택자가
`.container` 나 `a` 처럼 아주 일반적이다. 말풍선에 그냥 넣으면 그 스타일이 **문서 전체에** 적용돼 앱 레이아웃이 망가진다.
Shadow DOM 은 스타일 경계로 이걸 막아주면서, 위젯 HTML 자체는 그대로 유지한다 (요건 충족).
`attachShadow` 를 못 쓰는 환경이면 직접 넣는다 — 안 보이는 것보다는 낫다.

### 검색 중인 게 화면에 보인다

검색이 붙은 턴은 말풍선 머리가 **`🔍 자료 찾는 중…`** 으로 뜬다.
안건 응답이 1~3초 느려지는 이유가 드러나야 같은 기다림도 견딜 만해진다. 첫 글자가 나오면 원래 태그로 돌아간다.

---

## 4. 전문가 석 — 기본 off

`FEATURES.expertSeat = false`. 다섯 명이 문화·기술·경제·생활·법을 덮으므로 초빙석이 겹친다.

**코드는 전부 남겼다** — `EXPERT_POOL`, `buildExpertSystem`, `pickExpert`, `refineExpertTitle`,
`EXPERT_VOICE/HUMOR/SAMPLES`, CSS, TTS 프리셋. 플래그 한 줄을 `true` 로 바꾸면 v2.8 동작으로 그대로 돌아온다.

막는 건 **신규 입장뿐이다.** 옛 세션에 이미 앉아 있던 전문가는 그 방에서 계속 보이고 계속 말한다.

---

## 5. 비용 · 속도

**5인 개편 자체**
- 메시지당 콜 수 **변화 없음** (응답 인원 상한 그대로).
- 시스템 프롬프트 턴당 +약 150~250 토큰(3~5%). Anthropic 기준 1콜 20원 → 약 21원. 체감 없음.
- 라우팅은 전부 로컬 문자열 매칭 — 5벌 사전을 훑어도 0.1ms 미만.

**웹 검색**

| | 발동 | 건당 | 지연 |
|---|---|---|---|
| Gemini 3.x | 안건 턴의 1명만 | 월 5,000건 무료 → 이후 약 20원 | +1~3초 |
| Gemini 2.5 Flash | 〃 | 1,500 RPD 무료 → 이후 약 49원 | +1~3초 |
| Anthropic | 〃 (`max_uses` 4 → **2**) | 1콜 20원 → 대략 60~100원 | +2~5초 |

- 하루 100턴 중 안건이 15턴이면 **검색 15건/일 ≈ 450건/월** → Gemini 무료 한도 안에서 끝난다.
- 잡담·리액션에는 검색이 안 붙으므로 **평소 체감 속도는 v2.8 그대로**다.
- 한도에 걸리면 기존 폴백·쿨다운·우아한 축소가 그대로 처리한다.

---

## 6. 기존 세션 호환

`ARCHIVE_PERSONAS` 를 새로 만들었다. **이 표가 없으면 지난 세션의 정민철 말풍선이 이름 대신 `strategist` 로 깨진다.**

| 상황 | 처리 |
|---|---|
| 정민철 발언이 있는 방 | 이름·색(`#a3e635`)·태그까지 **지금과 똑같이** 표시 |
| 그 방에서 새 메시지 | 5인 중에서만 응답자가 뽑힌다. 정민철은 다시 말하지 않는다 |
| 정민철 말풍선 읽기(TTS) | 프리셋을 남겨둬서 **옛 목소리 그대로** |
| 대화방 목록 미리보기 | `sessionPersonaName` 도 아카이브를 조회한다 |
| v1.x id (`product`/`engineer`/`marketer`/`finance`) | 같은 표로 흡수 (동작 동일) |
| 저장된 아바타 | 서다현·이준호는 그대로. 신규 3인은 이니셜 기본값부터 |
| 마이그레이션 | **없다.** 옛 메시지의 `personaId` 를 건드리지 않는다 |

---

## 7. 검증

- 인라인 스크립트 문법 검사: **오류 0**
- 라우팅 엔진 헤드리스 실행: 무게 판정 · 담당자 선정 · 검색 발동 · 잡담 로테이션 (2절 결과표)
- 그라운딩 해석기: 출처 중복 제거 · `web` 아닌 청크 제외 · 검색어 추출 · **위젯 무수정 보존** 확인
- 위젯 크기 상한(12KB 초과 시 미탑재), 빈 메타데이터 방어 확인
- Shadow DOM 주입: 격리 ✓ / 중복 마운트 방지 ✓ / 미지원 폴백 ✓ / 검색 없는 메시지 무동작 ✓

**아직 실기기에서 확인하지 못한 것** (브라우저 확장이 연결돼 있지 않았다):
실제 Gemini 키로 검색을 태운 왕복, 구글 위젯의 실제 렌더 모양, 5인 아바타의 화면상 색 구분.
첫 실행 때 이 세 가지를 눈으로 확인하는 걸 권한다.

---

## 8. 되돌리는 법

| 원하는 것 | 방법 |
|---|---|
| 전문가 석 복귀 | `FEATURES.expertSeat = true` |
| 웹 검색 전체 off | `SEARCH_RULES.enabled = false` |
| 검색을 안건에서도 빼기 | `SEARCH_RULES.weights = []` |
| 구글 위젯 숨기기 | `SEARCH_RULES.showWidget = false` (※ 사용 요건 확인 필요) |
| 잡담에 5인 전원 등판 | `DOMAIN_RULES.smalltalkAll = true` |
| 담당자 우대 약화 | `DOMAIN_RULES.ownerBonus` 를 낮춘다 |
| 잡담 로테이션 약화 | `DOMAIN_RULES.smalltalkPenalty` 를 낮춘다 |
