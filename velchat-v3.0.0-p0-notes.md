# 벨챗 v3.0.0-p0 — LLM이 죽어도 앱은 산다

2026-08-18 · `vela-boardroom-prototype.html`

한 줄 요약: **429를 종류별로 갈라 보게 됐고, LLM이 전멸해도 앱이 정직하게 응답한다.
그리고 "모르는 건 모른다"를 대시보드에도 적용했다.**

---

## 0. 이번 버전의 제약 — 먼저 밝혀둔다

계획 단계에서 프리플라이트를 실측한 결과, **판별 가능성이 provider마다 다르다**는 게 드러났다.
이건 구현으로 우회할 수 있는 문제가 아니라서, 설계 자체를 여기에 맞췄다.

| provider | 판별 신호 | 결과 |
|---|---|---|
| Gemini | 본문 `error.details[].violations[].quotaId` | **확정** (한도 숫자까지) |
| Groq | 본문 메시지 `"...per day (RPD): Limit 14400, Used 14400"` | 추정 |
| Anthropic | 본문에 단서 없음 → 대기시간 길이로만 | 추정 |

**헤더는 못 읽는다.** Groq·Anthropic이 `Access-Control-Expose-Headers`를 주지 않아서,
브라우저 JS의 `res.headers.get()`은 CORS 안전목록 밖 헤더를 전부 `null`로 돌려준다.
`retry-after`조차 그렇다.

> 부수 발견: v2.9.2에서 넣은 `parseRetryAfterMs`의 헤더 분기는 **사실상 죽은 코드**였다.
> 실제로 동작한 건 본문 문자열 파싱뿐이다. 지우지는 않았다 — provider가 헤더를 열어주면
> 그대로 살아난다. 대신 **주석으로 "지금은 안 읽힌다"를 명시**해서,
> 다음 사람이 이 분기를 믿고 설계하지 않게 했다.

그래서 이 버전의 원칙은 하나다: **확정과 추정을 섞되, 섞였다는 걸 표시한다.**
추정을 확정처럼 보여주는 건 v2.9.1 환율 사고와 같은 종류의 잘못이다.

---

## 1. 오류 분류 8종 (`classifyError`)

```
429_RPM   분당 요청 한도  → 잠깐 기다리면 풀린다
429_RPD   일일 요청 한도  → 오늘은 끝났다. 기다려도 소용없다
429_TPM   분당 토큰 한도  → 잠깐 기다리면 풀린다
503       저쪽 서버 혼잡  → 짧게 한 번 더
TIMEOUT / NETWORK        → 짧게 한 번 더
AUTH / MODEL_404         → 재시도 무의미. 사용자가 고쳐야 한다
UNKNOWN                  → 그 외
```

판별은 3단으로 내려간다: **① Gemini 본문 구조(확정) → ② 본문 메시지 문자열(추정)
→ ③ 대기시간 길이(추정, 10분 이상이면 RPD 의심).**

분류는 **한 곳에서 한 번만** 한다(`callLLM`의 `!res.ok` 블록). 이후 재시도·안내·측정이
전부 그 결과를 참조한다 — 같은 오류를 여러 군데서 제각각 해석하면 판단이 갈린다.

라우팅 로그에도 그대로 찍힌다:

```
22:14 ✕ Gemini 강지원 429
      ⛔ 429_RPD 일일 한도 · 내일 오후 4시쯤 리셋
```

추정이면 `429_RPD 일일 한도 (추정)`으로 나간다.

### 리셋 시각은 서머타임까지 계산한다

Gemini 무료 티어 RPD는 태평양시 자정 리셋이다. **고정 오프셋(-8h)으로 계산하면
3월~11월 내내 한 시간씩 틀린다** — "내일 오후 4시"와 "5시"는 사용자에게 다른 말이다.
그래서 `Intl.DateTimeFormat`으로 그 시점의 실제 오프셋을 구해서 쓴다.
(실측: 8월 → 태평양시 정확히 자정 = 한국 오후 4시, 1월도 자정)

---

## 2. `FACT_RULES` 오류별 차등 — 절대 규칙을 완화했다

v2.9.5는 "Fact가 있으면 무조건 1회"였다. 단순해서 좋았지만 **너무 거칠었다** —
503 한 번에 설명을 포기하는 건 과하다.

```js
byKind: {
  '429_RPD':   { retry: 0 },                                        // 오늘은 끝났다
  'AUTH':      { retry: 0 },
  'MODEL_404': { retry: 0 },
  '429_RPM':   { retry: 1, waitMs: 'retryAfter', maxWaitMs: 15000 },
  '429_TPM':   { retry: 1, waitMs: 'retryAfter', maxWaitMs: 15000 },
  '503':       { retry: 1, waitMs: 2000, maxWaitMs: 4000 },
  'TIMEOUT':   { retry: 1, waitMs: 1500, maxWaitMs: 4000 },
  'NETWORK':   { retry: 1, waitMs: 1500, maxWaitMs: 4000 }
}
```

**`maxWaitMs`가 이 블록의 핵심이다.** Retry-After가 7분이라고 7분을 기다리면 안 된다 —
사용자에게 보여줄 숫자는 이미 손에 있다. 실측 검증: 450,000ms 요청 → 15,000ms로 잘린다.

규칙표에 없는 종류는 기존 기본값(재시도 없음)을 그대로 따른다. 즉 **v2.9.5 동작이 기본이고,
명시한 종류만 완화된다.**

---

## 3. 축소 모드 — 침묵하지 않는다

v2.8.3의 "우아한 축소"는 응답자를 줄이는 데서 끝났다. 다 막히면 앱은 침묵했다.
**사용자에게 침묵은 고장과 구별되지 않는다.**

| 상황 | 응답 |
|---|---|
| Fact 턴 + LLM 전멸 | 숫자 카드 (v2.9.4 그대로) |
| 일반 대화 턴 + LLM 전멸 | **시스템 안내 1회** — 페르소나 말풍선이 아니다 |
| 회복 | 자동. 안내 없음 |

```
지금 답변이 어려워요. 오늘 쓸 수 있는 양을 다 썼어요 · 내일 오후 4시쯤 다시 열려요
지금 답변이 어려워요. 잠시 요청이 몰렸어요 · 약 40초 뒤 다시 열려요
```

- **개발자 용어 금지(v2.9.2)를 그대로 적용한다.** 429·RPD·API·호출 같은 말이 한 글자도 안 들어간다.
- 추정이면 단정하지 않는다 — "다 쓴 것 같아요".
- 리셋 시점을 모르면 **시점을 아예 말하지 않는다.**

### 판단은 턴 끝까지 미룬다

예전엔 페르소나 하나가 실패하면 그 자리에서 칩을 띄웠다. 두 가지가 잘못이었다.
개발자 말이었고, **아직 끝난 게 아닌데 결론을 내버렸다** — 뒤에 최후의 한 명이 성공하면
방금 띄운 안내가 거짓말이 된다.

그래서 채팅 턴에서는 `degradeDeferred`로 보류하고, **최후의 한 명까지 실패한 뒤에만** 낸다.
(회의 모드 등 미룰 곳이 없는 호출부는 예전처럼 그 자리에서 낸다)

### 도배 방지

- 같은 상태(**종류 + 리셋 시점**)에서는 한 번만. 리셋 시점은 분 단위로 뭉갠다 —
  초 단위로 들고 있으면 매번 다른 열쇠가 되어 안내가 매 메시지마다 나간다.
- 상태가 같아도 최소 간격 3분(`DEGRADE_RULES.noticeGapMs`).
- **"다시 연결됐어요"는 내지 않는다.** 정상 복귀는 답이 오는 것으로 이미 드러난다.
  그걸 또 알리면 그것도 도배다.

입력창은 막지 않는다. 축소 모드 중에도 Fact 질문은 계속 답이 나간다.

---

## 4. 캐시 TTL — 데이터 성격에 맞춘다

| 소스 | 이전 | 지금 | 근거 |
|---|---|---|---|
| 환율 | 30분 | **6시간** | ECB 계열은 영업일 하루 한 번(16:00 CET) 갱신 |
| 날씨 | 10분 | **30분** | open-meteo 관측 갱신이 15분 단위 |

> "모든 캐시 5분" 같은 일괄 정책을 쓰지 않는 이유가 여기 있다.
> **환율에 30분은 과하게 짧고, 날씨에 6시간은 과하게 길다.**

**캐시가 오래돼도 위험하지 않다.** 출처줄에는 캐시 나이가 아니라 원 데이터의 `asOf`
("8월 14일 기준")가 그대로 나간다. 사용자가 보는 "언제 기준 값인가"는 캐시와 무관하게 정확하다.
v2.9.3에서 `asOf`를 넣어둔 덕을 여기서 본다.

적중 시 API 왕복은 생략되고(기존 동작), 그 절약을 이제 센다 — `CACHE_HIT_RATE`.
적중률이 낮으면 TTL이 데이터의 갱신 주기보다 짧다는 신호다.

---

## 5. 폴백 불변식 — 경로 전수 + 회귀 고정

> **Fact-required 질문에서 Fact를 확보하지 못했다면, 어떤 provider·어떤 폴백 경로로도
> LLM이 실시간 수치를 단정하는 출력이 나갈 수 없다.**

경로는 넷뿐이고, 코드로 확인했다:

| # | 조건 | 프롬프트에 반드시 들어가는 것 |
|---|---|---|
| A | Fact 확보 | `factDirective` — "이 숫자를 바꾸지 마라" + asOf |
| B | Fact 실패 + 검색 가능 | "네 기억과 검색 결과가 다르면 무조건 검색 결과가 맞다" |
| C | Fact 실패 + 검색 불가 | "아는 숫자를 현재값처럼 말하면 절대 안 된다" |
| D | 전부 실패 | LLM 출력 없음 → 숫자 카드 또는 축소 모드 안내 |

**두 번째 응답자도 같은 보호를 받는다.** `fact`와 `hasFact`는 `i === 0`에게만 가므로
2번째 이후는 경로 C를 탄다 — 회귀에 고정해뒀다.

**provider 불변성**: 프롬프트 조립은 provider를 인자로 받지 않는다(`factDirective(fact)`,
`buildChatInstruction(opts)`). 구조로 보장되지만, 나중에 누가 provider별 분기를 넣으면
즉시 잡히도록 회귀로 고정했다.

> 회귀에서 프롬프트 **전체 문자열 비교는 하지 않는다** — 마지막에 질문을 던질지가
> `Math.random`으로 갈리기 때문이다. 그건 연출이고 provider와 무관하다.
> 불변식은 "Fact의 값·출처·기준시점"이므로 그 부분만 정확히 비교한다.

---

## 6. 유료 판단 대시보드 (📊)

```
일일 한도
  gemini:flash — 한도 200회/일 · 소진 2회
  groq:oss     — 한도 미상 (브라우저에서 확인 불가) · 소진 1회
  최근 3일 중 소진 2일 (기준선 3일)
  전환 기준선: 14일 중 3일 소진 · 체감 실패 3%

오늘 (2026-08-18)
  메시지 24 · LLM 호출 27
  메시지당 1.13콜 (목표 1.0 이하)
  체감 실패율 4.2% (1/24)  기준선 3%
  체감 실패 = 정상 AI 응답을 못 받은 메시지 (숫자 카드·안내 포함)
  축소 응답 8.3% (2) · LLM 실패율 22.2%
  데이터조회 6(캐시적중 67% · 실패 0)
  오류 429_RPD 3 · 429_RPM 1 (그중 추정 1)
```

- **체감 실패율** = 정상 AI 응답을 못 받은 메시지 / 전체. **숫자 카드도 실패로 센다** —
  답이 나가긴 했지만 그건 앱이 낸 값이지 사용자가 기대한 대화가 아니다.
  정의를 화면에 함께 적는다. 정의를 모르면 4.2%가 좋은 건지 나쁜 건지 판단할 수 없다.
- **RPD 한도**는 확정된 것만 숫자로 쓴다. 나머지는 **"한도 미상"**.
  *앱이 사용자에게 요구하는 정직함을 대시보드에도 똑같이 적용한다.*
- **소진 일수**가 기준선(14일 중 3일)에 닿으면 "유료 전환을 검토할 시점입니다"가 뜬다.

### 측정 스키마 — 마이그레이션은 하지 않는다

`metricDay()`는 새 날짜에만 기본 객체를 만든다. 이미 저장된 지난 날짜에는 신규 필드가 없다.
읽는 쪽을 전부 `(d.x || 0)`으로 방어했고, **마이그레이션 코드는 넣지 않았다** —
`keepDays`가 14이므로 2주면 자연히 교체된다. 하루 증가분은 100~200바이트 수준.

`metricBump`은 평평해서 provider·종류별 집계를 못 담는다. `metricBump2(field, k1, k2)`를 추가했다.

---

## 7. 검증

**회귀 78건 전부 통과 · 실패 0** (`node` 하네스로 HTML에서 함수를 추출해 검사)

- 오류 분류 18건 — Gemini RPD/RPM/TPM 확정, Groq RPD/RPM 추정, 단서 없는 429의 대기시간 추정,
  AUTH·MODEL_404·503·529·UNKNOWN
- 차등 재시도 9건 — **Retry-After 7분 → 15초로 잘림** 포함
- 축소 모드 17건 — 안내 1회, 개발자 용어 0, 추정 시 단정 회피, 서머타임 리셋 시각
- 캐시 4건 · 불변식 11건(A/B/C/C2 + provider 불변) · 대시보드 11건 · v2.9.x 회귀 7건

**실기기 확인 완료** (이번엔 브라우저를 붙였다):

- 페이지 로드 — 콘솔 오류·경고 0건
- `classifyError` 실호출 → Gemini RPD 확정 + 한도 200 + 리셋 "내일 오후 4시쯤"
  (태평양시 정확히 자정으로 검증)
- 축소 모드 → 실제 시스템 말풍선 1개, 두 번째 호출은 억제됨
- 라우팅 로그 → `⛔ 429_RPD 일일 한도 · 내일 오후 4시쯤 리셋` 출력 확인
- 대시보드 → 한도 확정/미상 구분, 기준선 표시 확인

문법 검사 통과.

---

## 8. 조정하는 법

| 원하는 것 | 방법 |
|---|---|
| 축소 모드 안내 끄기 | `DEGRADE_RULES.notice = false` |
| 안내 간격 조절 | `DEGRADE_RULES.noticeGapMs` (기본 3분) |
| 회복 안내 켜기 | `DEGRADE_RULES.recoveryNotice = true` (권하지 않음) |
| 한도 대기 상한 조절 | `FACT_RULES.byKind['429_RPM'].maxWaitMs` |
| 특정 오류에 재시도 추가 | `FACT_RULES.byKind` 에 한 줄 |
| RPD 추정 기준 조절 | `ERROR_RULES.rpdHintMs` (기본 10분) |
| 캐시 TTL | `DATA_SOURCES.fx.ttlMs` / `.weather.ttlMs` |
| 전환 기준선 | `METRICS.baseline` |

---

## 9. 다음에 확인할 것

실제로 한도를 만났을 때 **설정 → 🔀 최근 라우팅**에서:

- `429_RPD`가 **확정**으로 찍히는가(Gemini), **추정**으로 찍히는가(Groq)
- 리셋 시각 안내가 실제 복구 시점과 맞는가
- 축소 모드 안내가 그 상태에서 **딱 한 번만** 나오는가

📊 측정에서 14일이 쌓이면 RPD 소진일 수가 유료 전환 판단의 실제 근거가 된다.
그때까지는 **RPD가 병목이라는 가설이 아직 증명되지 않은 상태**라는 걸 기억해둔다 —
이번 버전은 그 증거를 모으기 위한 계측이 절반이다.
