# 벨챗 v2.8.2 — Gemini 모델명 갱신 + 모델 은퇴에 견디는 구조

v2.8.0 은 `gemini-2.5-flash` 처럼 **세대를 못 박은 이름**을 기본값으로 썼다.
구글이 그 모델을 내리는 순간 앱은 404 를 뱉고, 사용자는 이유를 알 수 없고,
**앱 코드를 고쳐 재배포하기 전까지 아무도 못 쓴다.**

이번 버전은 그 이름 하나를 바꾸는 게 아니라, **같은 일이 다시 나도 앱이 버티는 구조**를 만든다.

---

## 문제의 성격 — 이건 반복되는 일이다

모델은 언젠가 반드시 은퇴한다. Groq 의 `llama-3.1-8b-instant` 도 그랬고
(v2.8 의 `MODEL_MIGRATIONS` 가 그 흔적이다), Gemini 도 그럴 것이고, 다음도 그럴 것이다.

이름 하나를 새 이름으로 바꾸는 건 **이번 사고를 끄는 것**이지 고치는 게 아니다.
매번 앱을 재배포해야 한다면 그건 사용자가 기다려야 하는 구조다.

그래서 세 겹으로 막는다.

- **① 별칭** — 발급처가 "항상 최신"으로 관리해주는 이름을 기본값으로. 은퇴가 자동 흡수된다.
- **② 사용자 수정** — 설정에서 모델 이름을 직접 칠 수 있게. 코드 수정 없이 대응.
- **③ 자기회복** — 404 가 나면 목록 API 로 "지금 쓸 수 있는 이름"을 물어다 보여준다.

①이 대부분을 막고, ②가 ①이 못 막는 걸 막고, ③이 ②를 할 수 있게 만든다.

---

## ① 별칭으로 갈아탄다

```js
gemini: {
  defaultModel: 'gemini-flash-latest',        // 기존: gemini-2.5-flash
  fastModel:    'gemini-flash-lite-latest',   // 기존: gemini-2.5-flash-lite
}
```

`-latest` 계열은 구글이 최신 세대를 가리키도록 관리한다.
2.5 가 은퇴하고 3.0 이 나와도 **우리 쪽 코드는 그대로 있으면 된다.**

2단 구성(v2.8)의 소형 모델도 같은 방식의 라이트 별칭이 있어 함께 옮겼다.
`endpointFor()` 안의 폴백 문자열도 잊지 않고 갈았다 —
`model` 이 비어 들어오는 경로에서 옛 이름이 되살아나면 같은 사고가 난다.

모델 목록에는 별칭 셋을 앞에 두고, 세대 고정 이름 둘을 뒤에 남겼다.
"굳이 그 세대를 쓰고 싶다"는 선택도 유효하기 때문이다.
별칭에는 `alias: true` 를 달아 드롭다운에 **⟳** 뱃지로 표시한다 —
고를 때 "이건 은퇴에 자동 대응된다"를 알 수 있어야 한다.

### `defaultModel` 을 따로 둔 이유

예전에는 `cfg.models[0].id` 가 곧 기본값이었다.
그러면 **목록 순서를 바꾸는 순간 기본값이 조용히 따라 바뀐다.**
"권장 모델을 위로 올리자"는 사소한 편집이 동작 변경이 되는 건 위험하다.
`defaultModel` 을 명시적으로 분리했다. 세 provider 모두 갖는다.

---

## ② 모델 이름을 직접 칠 수 있게

### 먼저 발견한 진짜 장애물

UI 를 만들기 전에 이걸 먼저 고쳐야 했다.

```js
// v2.8.1 까지 — loadProviderKeys()
const validIds = LLM_CONFIG[state.provider].models.map(m => m.id);
if (!saved || !validIds.includes(saved)) {
  saved = LLM_CONFIG[state.provider].models[0].id;   // ← 목록에 없으면 지운다
}
```

**입력칸을 아무리 잘 만들어도, 직접 친 모델명이 새로고침마다 날아간다.**
"우리가 아는 목록에 없으면 잘못된 값"이라는 전제가 깔려 있었는데,
모델 이름의 유효성은 우리가 아니라 **서버가** 판정한다.
v2.8.1 에서 API 키를 두고 배운 것과 정확히 같은 실수다.

```js
// v2.8.2 — 목록에 없다고 지우지 않는다
const moved = (MODEL_MIGRATIONS[state.provider] || {})[saved];
if (moved) saved = moved;
state.model = saved || defaultModelFor(state.provider);
```

이제 `MODEL_MIGRATIONS` 에 **명시적으로 적힌 것만** 옮긴다.
여기 없는 이름은 목록에 없어도 그대로 둔다 — 사용자가 직접 넣은 최신 모델일 수 있다.

마이그레이션 표에는 이번에 두 줄이 늘었다.
`gemini-2.0-flash` → `gemini-flash-latest`, `gemini-2.5-flash-lite` → `gemini-flash-lite-latest`.
**기존 사용자를 세대 고정에서 별칭으로 옮겨주는 것**이라, 다음 은퇴 때 그들도 안전해진다.
(`gemini-2.5-flash` 는 일부러 안 옮겼다 — 목록에 살아 있는 유효한 선택이다.)

### UI

드롭다운 마지막에 `✎ 직접 입력…` 을 붙였다. 고르면 텍스트 칸과 **↺**(기본값 복귀) 버튼이 열린다.
저장된 모델이 목록에 없으면 자동으로 이 모드로 열린다 — 사용자가 뭘 쓰고 있는지 보여야 한다.

값을 읽는 곳은 `chosenModelValue()` 하나로 모았다.
예전에는 저장 핸들러와 연결 테스트가 각각 `$('#inputModel').value` 를 직접 읽었는데,
그대로 두면 **연결 테스트는 드롭다운 값으로, 저장은 입력칸 값으로** 하는 어긋남이 생긴다.
테스트에 "옛 직접 읽기가 남아 있지 않은지" 검사를 넣어 고정했다.

힌트 줄은 기본값과 현재값을 같이 보여주고, 별칭이면 그 사실을 알려준다.

> 기본값 `gemini-flash-latest` — **⟳ 별칭**이라 모델이 은퇴해도 자동으로 최신 세대를 따라갑니다.

세 provider 모두 같은 UI 를 쓴다 (요청 4). provider 별 분기가 없다.

---

## ③ 404 자기회복 — 고칠 재료를 같이 준다

404 는 **사용자가 스스로 고칠 수 있는 유일한 오류**다. 이름만 바꾸면 된다.
문제는 "그럼 무슨 이름을 넣어야 하냐"를 앱이 안 알려준다는 것이었다.

그래서 키로 발급처에 직접 물어본다.

```js
async function fetchModelList(provider, key) {
  const res = await fetch(cfg.modelsUrl, { headers: providerHeaders(provider, key) });
  if (provider === 'gemini') {
    return (data.models || [])
      .filter(m => m.supportedGenerationMethods.indexOf('generateContent') >= 0)
      .map(m => m.name.replace(/^models\//, ''));
  }
  return (data.data || []).map(m => m.id);   // Anthropic · Groq 은 같은 형태
}
```

Gemini 는 `models/` 접두어를 붙여 주므로 떼어내고, **임베딩 전용 모델을 걸러낸다** —
`embedding-001` 을 채팅 모델로 제시하면 사용자를 다음 오류로 안내하는 꼴이다.
`providerHeaders()`(v2.8) 를 그대로 재사용해 인증이 어긋날 여지를 없앴다.

조회 실패는 전부 `null` 로 삼킨다. **구제 기능이 실패해서 앱이 더 망가지면 안 된다.**

### 어디서 뜨나 — 두 곳

**설정에서** — 연결 테스트가 모델 문제로 실패하면 자동으로 목록을 붙인다.

```
✕ 모델 이름을 확인해주세요 — gemini-2.5-flash 을(를) 찾을 수 없습니다

이 키로 지금 쓸 수 있는 모델 — 눌러서 넣으세요
gemini-flash-latest · gemini-pro-latest · gemini-2.5-flash · …
```

**이름을 누르면 그대로 모델 칸에 꽂힌다.** 읽고 옮겨 적게 하면 오타가 난다.
결과는 매번 새로 그려지므로 이벤트는 `#keyStatus` 에 위임했다.
`📋 쓸 수 있는 모델 조회` 버튼으로 오류 없이도 언제든 볼 수 있다.

**대화 도중에** — 404 가 나면 페르소나마다 같은 오류 말풍선이 뜬다. 그건 증상일 뿐이다.

```
모델 "gemini-2.5-flash" 이(가) 없어졌습니다.
지금 쓸 수 있는 것: gemini-flash-latest, gemini-pro-latest, … — 설정 → Model 에서 바꿔주세요.
```

`modelRescueDone` 플래그로 **한 번만** 띄운다. 세 명이 답하는 중이면 같은 칩이 세 번 뜬다.
모델을 저장하면 플래그가 풀려 다음에 또 문제가 생기면 다시 안내한다.

오류 분류는 `isModelUnknownError()` 하나로 모았다.
Gemini 는 `NOT_FOUND` 문구, Groq 은 `decommissioned`, 나머지는 404 —
표현은 달라도 사용자가 할 일은 같으므로 한 곳에서 판정한다.

**자동으로 바꾸지는 않는다.** 모델은 품질·비용·속도가 다 다르다.
앱이 마음대로 갈아끼우면 사용자가 모르는 사이에 답변 품질과 요금이 바뀐다. 고르는 건 사람이 한다.

### 목록 정렬 — 고르기 좋은 순서로

발급처가 주는 순서는 사람이 고르기 좋은 순서가 아니다. `rankModelNames()` 가 다시 세운다.

1. 별칭(`-latest`) — 다시는 은퇴로 안 깨지는 선택지를 맨 앞에
2. 우리가 아는 후보 — 라벨과 설명이 붙어 있는 것들
3. 나머지는 이름 역순 (대개 최신 세대가 뒤에 온다)

---

## 확인한 것

### 자동 테스트 — 47건 통과 (`test-v282.js`)

- **별칭 전환 (9)** — 기본·소형 모델이 별칭인지, 목록에 실제로 있는지(2단 안내가 라벨을 못 찾으면 깨진다),
  `alias` 표시, 세 provider 의 `defaultModel`·`modelsUrl`, `endpointFor` 폴백까지
- **모델 보존 (8)** — 강제 초기화가 사라졌는지, 마이그레이션 표만 적용되는지,
  **직접 입력한 미지의 모델이 살아남는지**(이번 수정의 핵심)
- **정렬 (4)** — 별칭 우선, 원본 배열 비파괴
- **404 판정 (6)** — Gemini `NOT_FOUND` / Groq `decommissioned` / 404,
  키 오류·한도 초과를 모델 문제로 오인하지 않는지
- **목록 파싱 (7)** — provider 3종 응답 형태, **임베딩 모델 제외**,
  HTTP 실패·네트워크 예외·키 없음에서 `null` 반환(throw 안 함)
- **UI 배선 (13)** — 요소 5개, `chosenModelValue` 일원화, 옛 직접 읽기 제거,
  구제 플래그와 리셋, 목록 클릭 위임

### 회귀 — 112건 통과

`regress.js` 37 · v2.8 테스트 23 + 21 · v2.8.1 테스트 31. 전부 그대로 통과.
`<style>` 중괄호 균형, `<script>` 두 블록 파싱도 통과. **합계 159건.**

### 실호출은 못 했다

**API 키가 없어 실제 호출은 하지 못했다.** 목록 파싱은 각 provider 문서 형태에 맞춘
합성 응답으로 검증했지만, 진짜 응답으로는 확인이 필요하다.
특히 `gemini-flash-latest` / `gemini-flash-lite-latest` **별칭이 실제로 존재하는지 확인하지 못했다** —
만약 이름이 다르면 `📋 쓸 수 있는 모델 조회` 가 바로 정답을 알려주도록 만들어 둔 게 이번 작업이다.
그게 이 구조의 요점이기도 하다: 우리가 틀려도 앱 안에서 고칠 수 있다.

**남은 확인 목록**

1. Gemini 키로 대화 → `gemini-flash-latest` 가 실제로 응답하는지
2. 설정 → `📋 쓸 수 있는 모델 조회` → 목록이 뜨고 별칭이 맨 앞에 오는지
3. 목록의 이름 클릭 → 모델 칸에 꽂히고 저장되는지
4. 모델을 `없는-모델-이름` 으로 저장 → 연결 테스트가 실패하며 **목록을 함께** 보여주는지
5. 그 상태로 대화 시도 → 시스템 칩이 **한 번만** 뜨는지
6. `✎ 직접 입력…` 으로 임의 이름 저장 → **새로고침 후에도 남아 있는지** (v2.8.1 까지는 날아갔다)
7. 기존 사용자 시뮬레이션 — `localStorage` 의 모델을 `gemini-2.0-flash` 로 두고 새로고침 →
   `gemini-flash-latest` 로 자동 이동하는지
8. `↺` 버튼 → 기본값으로 돌아가는지
9. Anthropic·Groq 에서도 2~4 가 같은 방식으로 동작하는지
10. **회귀** — 2단 모델 안내(v2.8) · 스트리밍 · 창 모드(v2.8.1) · 키보드(v2.7.2)

---

## 손댄 곳

| 파일 | 내용 |
|---|---|
| `vela-boardroom-prototype.html` | Gemini 별칭 전환 · `defaultModel`/`modelsUrl` · `MODEL_MIGRATIONS` 확장 · 모델 강제 초기화 제거 · 직접 입력 UI · `fetchModelList`/`rankModelNames`/`rescueUnknownModel` · 버전 2.8.2 |
| `vela-boardroom-sw.js` | 스탬프가 `BUILD_ID` 갱신 |
| `velchat-v2.8.2-notes.md` | 이 문서 |
